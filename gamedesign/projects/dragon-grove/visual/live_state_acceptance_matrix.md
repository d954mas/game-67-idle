# Dragon Grove Live-State Acceptance Matrix

Machine input: `visual/live_state_acceptance_matrix.json`.

First slice must cover `first_screen`, `hud_visible`, `primary_action_ready`,
`primary_action_feedback`, and `locked_or_disabled_state` before product pass.
