# Game-local Canvas Projects

Game-owned Canvas working projects live here when art work should stay with
`rb-dark-rpg`.

Project folders under this directory are ignored by the parent repository by
default. Publish sanitized runtime assets through `assets/` or `design/`
instead of committing raw Canvas working copies.
