build/
tmp/
.cache/
.vs/
.vscode/.cmake/
compile_commands.json
*.user
*.suo
*.ntpack
src/generated/
__pycache__/
*.pyc

# Builder scratch for asset packs
assets/runtime/*/_cache/
assets/packs/
*.png.ppm

# Restricted (paid/licensed) asset binaries — never commit to an open repo.
# Only the catalog/license .md (metadata, no binary) is committed; the binaries
# live here locally and in the private library. See tools/assets/restricted.mjs.
# Per-game: matches <game>/assets/restricted/ in any game folder.
**/assets/restricted/

# downloaded tunnel binary (app-tunnel skill)
tools/bin/
template/build/
template/src/generated/
