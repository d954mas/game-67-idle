# Tasks

Owns durable work state and orchestration:

- task lifecycle;
- status and evidence;
- subagent packet generation/checking;
- review queues;
- handoff records.

Current source material lives in `tasks/` and `tools/taskboard/`.

