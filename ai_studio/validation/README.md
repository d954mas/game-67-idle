# Validation

Owns mechanical quality gates:

- doc/reference checks;
- context budgets;
- product gates;
- visual/readability gates;
- repeated-failure stops;
- pipeline validation routing.

Current source material lives in `docs/ai-pipeline/quality-validation.md`,
`tools/product_gate/`, `tools/context_budget.mjs`,
`tools/doc_reference_check.mjs`, and `tools/pipeline_validate.mjs`.

