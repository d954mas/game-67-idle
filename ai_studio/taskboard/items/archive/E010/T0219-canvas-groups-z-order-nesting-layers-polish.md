---
id: T0219
title: "Canvas groups: z-order among siblings, NESTED groups, layers indent polish"
status: done
project: P001
epic: E010
priority: P1
tags: []
created: 2026-07-02
updated: 2026-07-10
---

## What

Lead's live feedback (2026-07-02) after T0217 landed. (1) GROUP Z-ORDER:
groups need the same sibling reordering elements got — drag in layers +
Order actions + shortcuts; extend `reorderElement` or add a sibling op for
groups (T0217 shipped elements-only, documented follow-up). (2) NESTED
GROUPS ("группы могут быть вложенными"): the model currently allows ONE
level (elements->group); extend to groups-in-groups. This is a real model
change — parent ref on groups, recursion in: composite render (renderGroup
must render child groups), layers panel tree, reparent drag (group onto
group), marquee/selection, move-translates-members, visibility cascade,
clip-to-frame later (T0213). Design first (deep-reasoner): keep the flat
elements[] z-order as the single paint order or move to a scene-graph
order? — decide explicitly, journal/undo untouched, tool parity (CLI
group-assign accepts group ids). (3) LAYERS INDENT POLISH: ungrouped
element rows carry a caret-width left spacer that reads as group
membership ("какой-то странный сдвиг слева") — visually distinguish
top-level rows from group members (no fake indent; members indent, roots
don't).

## Done when

- [x] groups reorder among siblings (drag with insertion line + Order actions + Ctrl+[/]); render/export honors the order; one journal entry per gesture
- [x] groups nest: create/move a group inside a group; composite render + visibility + move cascade correctly; layers tree shows the hierarchy; CLI parity
- [x] Figma selection model (lead decision 2026-07-02): canvas click selects the TOP-MOST group under cursor; double-click drills one level (screen -> widget group -> element); double-click on a leaf element = region-edit (unchanged); Ctrl+click = deep-select the leaf directly; marquee selects at the current scope
- [x] top-level rows have no fake indent; group members clearly indented (landed live 4c5b6815, depth-aware since inc1)
- [x] group CLIP: a group can clip members at its bounds (Figma frame behavior) — canvas draw + renderGroup honor the flag; toggle in inspector; default off
- [x] group BACKGROUND: optional fill on the group (color first), drawn on canvas AND composited by renderGroup (replaces the render-only background param as the primary path)
- [x] sliceRegions wraps its crops into a new group "<sheet name> slices" in the SAME op/journal entry (undo removes group+crops together); site + CLI parity (landed ahead, 59eb9df0)
- [x] deleteGroup DELETES its member elements with it (one journal entry, undo restores both); dissolving a group without deleting content stays on Ungroup (59eb9df0; full-subtree since inc3)
- [x] tests for group reorder + nested render + clip + background + slice-group; validate_map --strict + doc_reference_check green (suite 159/159)

## Open questions

## Log
- 2026-07-02: Created from live feedback during T0217 review.
- 2026-07-02: Lead: inspector Regions section "Edit" and "+ Add" buttons are redundant ("избыточно") - dblclick/context menu enter the mode, drag draws a rect (and T0209 brings the tool row). REMOVE both in the bug-fix acceptance pass; Detect and Slice buttons stay (no other UI path). Same pass: kill the fake left indent on top-level rows (item 3 of What).
- 2026-07-02: Lead: slice belongs in the region-mode right-click menu, count-aware - ONE region selected -> "Slice region", several -> "Slice selected (N)" (slices the SELECTION, not just the clicked row). Add in the bug-fix acceptance pass (context_menu.js busy).
- 2026-07-02: Lead (evening live testing): (a) slice dumps N crops straight onto the scene ("сразу 43 обьекта прямо на сцене") -> sliceRegions must create a wrapping group in the same journaled op; SMALL, pull forward: implement right after T0220 lands (ops.mjs is in the sweep's hands until then). (b) groups must be able to CLIP members at their bounds ("нужна возможность чтобы группа клипала по границам"). (c) From "почему я не вижу фон в канвасе": renderGroup's background is a render-time param only; groups need a real background (canvas-visible + render parity). (d) Figma reference discussed: images can't parent other layers in Figma - containers are frames/groups; a frame can carry an image fill. Design implication: keep groups as the only container, give them fill (color now, image later) instead of inventing image-parents.
- 2026-07-02: Lead: deleting a group must delete everything inside ("при удалении группы я ожидал что все вложенное удалится") - Ungroup is the explicit way to dissolve. Today deleteGroup orphans members to top level (ops.mjs:660-663). Pull forward with slice-group: implement right after T0220 frees ops.mjs.
- 2026-07-02: Indent item (3) already fixed live in 4c5b6815 (margin removed, caret spacer alone indents members); Edit/+Add removal + count-aware slice menu landed earlier same day. Remaining scope = groups z-order/nesting/clip/background + slice-group.
- 2026-07-02: Lead on group selection/move (design input): "если это составная кнопка я бы хотел как-то легко двигать группу. сейчас группа двигается за край что немного не очевидно и не удобно" - moving a group only by its frame edge/label is not acceptable for composite-widget groups (button = group). Selection model must give an easy whole-group move; feeds the click-selects-what decision in the v2 design.
- 2026-07-02: DESIGN SETTLED (two independent deep-reasoner designs synthesized + two lead decisions). MODEL (lead confirmed "разве это не решается просто полем родитель? у дефолда плоский массив"): NO stored tree - flat elements[]/groups[] stay, additive optional fields: group.parentId (null=root), order (z among same-scope siblings, elements+groups merged) on both, group.clip (bool, default false), group.background ({type:"color",color}|null). Paint order COMPUTED by recursive per-scope sort in a new shared pure module tree.mjs (imported by ops.mjs AND site - ordering itself obeys tool parity). Undo free (snapshotOf already captures both arrays); v1 projects open with ZERO converter (no order -> fallback: element key = array index, group key = min member index; lazy normalization on first reorder per scope). Children-arrays variant REJECTED (second source of truth). SELECTION (lead picked Figma-full over Alt-drag/innermost-group/PS-toggle): click = top-most group, dbl-click drills a level, leaf dbl-click = region-edit, Ctrl+click = deep-select leaf. OPS: reorderNode (merged siblings, one entry), reparentGroup (cycle guard: reject self/descendant target), createGroup parentId, patchGroup clip/background + move cascade over FULL descendant closure, deleteGroup deletes subtree; HTTP+CLI parity for all. RENDER: compositeGroup builds nested spec, render_group.py recursive painter (background fill first in group band, clip = own layer sized to intersected clip box); explicit --background arg overrides group.background; exportProject = visible TOP-LEVEL groups only (screen = top-level group; nested = components). CLIP UX: hard clip, canvas-only display flag (export always crops at bounds already), clipped-out pixels not hit-testable, selected member with overflow shows ghosted silhouette (anti "lost my sprite" trap). BACKGROUND UX: fill behind children canvas+export, hairline outline + label stay (empty screens visible), filled body click = selects group. UX TRAPS recorded: insertion-line indent must encode target parent, no-drop state on descendant targets, drag hysteresis. INCREMENTS: 1) tree.mjs foundation + read path (canvas 2-pass paint, recursive layers tree, visibility ancestor walk, v1 fallback) + BACKGROUND (patchGroup field + canvas fill + render parity + inspector section) - instant visible payoff; 2) group z-order (reorderNode + drag insertion + Order actions + Ctrl+[/] on groups); 3) nesting (reparentGroup, cascades, recursive render, drag-to-nest, Figma selection/drill model); 4) clip (+ ghost hint). Full agent reports in session transcripts; distilled here as the authoritative record.
- 2026-07-02: INCREMENT 1 LANDED (deep-reasoner, accepted, commit 64965a8d). tree.mjs shared pure ordering module (childrenOf returns {elements,groups} native order backing member helpers; orderedChildren = merged tagged nodes back->front, explicit order only when EVERY sibling has it; empty group -> front via +Infinity guarded comparator; dangling parent -> root defensively, cycle-capped walks). Canvas = 2-pass paint (recursive artwork + chrome overlay, groupLabelRects intact); layers = recursive renderScope front-at-top w/ depth indents; visibility cascades (isNodeHidden). Background: patchGroup validates loudly, background:null DELETES field (None on unfilled group = genuine no-op, no journal entry), render_group.py needed NO change (spec.background already bottom fill), compositeGroup precedence arg > group.background > transparent; inspector Background section + context-menu Pick color (throwaway native color input) + CLI group-set --background. Tests 110->126 (tree.test.mjs new; PIL pixel tests for fill + override). Gates re-run at acceptance: 126/126, map strict 297, docs ok. Deferred as designed: reorderNode (inc2), reparentGroup/cascades/drag-to-nest/Figma-selection (inc3), clip (inc4); hitElement still elements[]-reverse until inc3; exportProject top-level-only filter waits for nesting.
- 2026-07-02: INCREMENT 2 LANDED (deep-reasoner, accepted, commit 5e7a037e). reorderNode({nodeId,index}) over MERGED same-scope siblings (orderedChildren visual index, 0=back); first reorder lazily normalizes THAT scope to explicit contiguous order; strict errors; one entry. reorderElement = forgiving delegate (clamps). frontOrder hook at ALL insertion points (addImage/assignToGroup/createGroup/sliceRegions - agent extended beyond packet to protect the never-half-explicit invariant; no-op on untouched scopes). compositeGroup composites members in computed z-order (PIL test proves it). tree.mjs += nodeScope/frontOrder. HTTP /nodes/<id>/reorder; CLI node-reorder. Page: node-based Order actions, group "Order >" submenu, Ctrl+[/] on selected group or single element (MIXED multi-selection = no-op BY DESIGN - one gesture must stay one journal entry; batched reorderNodes op = future follow-up if lead wants multi-nudge), group rows draggable for sibling reorder w/ insertion line, group-onto-group drop = suppressed no-op until inc3; element drag onto group header stays "join". reorderNode writes order FIELDS, never permutes elements[] (groups aren't in that array). Tests 126->134. Gates re-run at acceptance: 134/134, map strict, docs ok.
- 2026-07-02: INCREMENT 3 LANDED (deep-reasoner, accepted, commit f9e9ec0f). NESTING: createGroup --parent (fromElements defaults to members' COMMON groupId else root), reparentGroup w/ wouldCycle guard (self/descendant/unknown all throw) + never-half-explicit order normalization, patchGroup move cascades FULL descendant closure, deleteGroup removes whole subtree one entry (returns removedGroups+removedElements, undo restores all), Ungroup = dissolve ONE level up then delete empty group (page-composed, rewritten because deleteGroup now cascades). RENDER: buildRenderNodes emits recursive z-ordered children tree; render_group.py recursive painter w/ clean clip seam for inc4; hidden subtrees pruned; exportProject = visible TOP-LEVEL groups only. SELECTION (Figma model as lead decided): enteredGroupId scope state, hit-test fixed to computed z-order (was elements[]-reverse), click = top-most ancestor group at scope, dblclick drills one level (leaf dblclick = region-edit unchanged), Ctrl+click deep-select, Esc steps up scope (inserted AFTER region-edit unwinding, BEFORE clearSelection), empty true-click (<3px, non-shift) exits to root, marquee selects at scope, breadcrumb chip #scope-breadcrumb, hover outline. LAYERS: header middle(75%)=nest-into vs edge(25%)=sibling reorder from one drag; dragged group's own subtree = inert targets; nested Move-to-group submenus indented. KNOWN COMPROMISES (documented): mixed marquee move = N+1 journal entries (one elements-set batch + patchGroup per group; batched mixed-move op = follow-up); Ungroup children land at parent-scope front, not former z-slot; 2+ group selection shows empty inspector; filled-body click relies on member-content hit (container resolved via Figma rule). Tests 134->150 (nesting.test.mjs 14 new). Gates re-run at acceptance: 150/150, map strict 297/397, docs ok.
- 2026-07-02: INCREMENT 4 LANDED (deep-reasoner, accepted, commit 7a4a2a49) - T0219 COMPLETE, -> review. CLIP: patchGroup {clip} strict boolean (loud error), clip:false DELETES field (absent=off, mirrors background convention, no-op journal-wise on untouched groups). Canvas: ctx clip stack around clipping group's fill+children (nested clips intersect via stack); clipped-out pixels excluded from hitElement (whole-subtree skip when point outside clipping ancestor) AND marquee (visibleBox = box intersect all clip ancestors); GHOST HINT: selected content overflowing a clip redrawn at 0.25 alpha via even-odd clip in chrome pass (image ghost chosen over dashed outline - shows WHAT is hidden; group selection ghosts member images, frames are always-drawn chrome). Render: recursive painter, clip:true subgroup composited on own box-sized layer then pasted (paste-crop chain = provably correct nested intersection, 2-level pixel test); export bounds UNCHANGED (flag governs subgroup overflow + canvas display). Inspector Clip content checkbox next to Visible; context-menu toggle w/ check prefix; CLI group-set --clip true|false (op strict boolean, CLI converts). groupAtCenter drop-reparent left clip-unaware BY DESIGN (dropping inside a frame intended). Tests 150->159 (clip.test.mjs). Gates at acceptance: 159/159, map strict, docs ok. Server :8780 restarted. AWAITING lead live verification (checklist sections inc1-4).
- 2026-07-11: T0375 status reconciliation: done; all 9 acceptance criteria are checked and the card log contains nested-group/z-order verification evidence.
