---
id: T0441
title: "template node tools: probe studio root depth like cmake (private games/<id> crash)"
status: review
project: P001
epic: E009
priority: P2
tags: [template, tooling, private-games]
created: 2026-07-17
updated: 2026-07-17
---

## What

`templates/template/tools/{game,package_web,portal_evidence}.mjs` (and their
`.test.mjs` files) hardcode a depth-2 studio root: `resolve(gameDir, "..", "..")`
plus the static import
`import { inspectPlatformSdkArtifact } from "../../../features/platform-sdk/scripts/artifact_tools.mjs"`
(`package_web.mjs:20`). For a private game copied to `games/private/<id>`
(depth 3), that import resolves to the non-existent `games/features/...`, so
EVERY `node tools/game.mjs ...` command dies at module load with
`ERR_MODULE_NOT_FOUND` — doctor/test/build/package/verify are all unusable.

The game-side cmake already solved the same problem: `cmake/GameOptions.cmake`
probes for `external/neotolis-engine` at depth 2 then depth 3 to set
`GAME_REPO_ROOT`. The node tools never got the equivalent.

A proven fix exists in the affected private game
(2026-07-17): new `tools/lib/studio_root.mjs` exporting `findStudioRoot(gameDir)`
(same probe as cmake), all `resolve(gameDir, "..", "..")` call sites and
`new URL("../../..", import.meta.url)` defaults switched to it, the static
artifact_tools import replaced with a top-level-await dynamic import from the
probed root, and the `portal_evidence.test.mjs` fixture extended to copy
`lib/studio_root.mjs` + create the `external/neotolis-engine` marker. All 41
node tool tests pass at depth 3, and the depth-2 fixture tests still pass.

## Done when

- [x] `templates/template/tools/` carries the studio-root probe (port the
      game-side `tools/lib/studio_root.mjs` approach) and no tool or test file
      hardcodes `../../..` / `resolve(gameDir, "..", "..")` for the studio root.
- [x] From a depth-3 checkout (`games/private/<id>`), `node tools/game.mjs doctor`
      reaches dependency verification instead of crashing at import.
- [x] Template node tests stay green at depth 2 (templates/template) — the
      fixture layouts in `game.test.mjs` / `portal_evidence.test.mjs` cover the
      probe (engine marker present in fixture roots).

## Open questions

- Should `new_game.mjs` copy games into `games/private/` get a smoke check that
  runs `node tools/game.mjs doctor` once, so this class of layout break fails at
  scaffold time instead of first use?

## Log

- 2026-07-17: filed from the affected private-game fix session; the game-side
  copy of the fix is live in that game's repo (`tools/lib/studio_root.mjs`).
- 2026-07-17: Selected after E016/E019 closeout. First prerequisite: redact private-game identifiers from T0440/T0441 and prove privacy preflight with process-scoped safe.directory; private repository metadata remains untouched.
- 2026-07-17: Privacy hygiene complete: T0440/T0441 retain technical evidence with private identifiers redacted; workspace preflight passes when sandbox Git receives the private root as process-scoped safe.directory.
- 2026-07-17: Implementation complete: template Node tools now resolve Studio root by probing the public and private game depths, package_web loads artifact_tools from that resolved root, and copied-game fixtures cover games/private/<id>. The complete template Node suite passes 43/43; independent review found no actionable path, platform, security, or coverage issues.
- 2026-07-17: Non-blocking scaffold-smoke question remains recorded for future routing; this slice does not expand scope because the copied depth-3 doctor/package fixture already fails at module load if root probing regresses.
- 2026-07-17: Quality: QTECH_001=pass; evidence: RED reproduced depth-3 ERR_MODULE_NOT_FOUND; focused probe/depth-3 CLI suite passed 10/10; complete template Node suite passed 43/43; changed verify passed template-release/work-management; final full Studio verify passed all 10 domains; Taskboard and diff checks are clean; independent review found no actionable issues.
- 2026-07-17: CI run 29616332944 exposed one harness ownership omission on both OS jobs: the new tracked studio_root.test.mjs was not registered in the reference-template owner suite. The implementation and template-release domain passed. Registered the test in the existing owner suite; focused harness passes 25/25 and template Node remains 43/43.
- 2026-07-17: CI-fix verification: harness suite passed 25/25 with deterministic-test ownership clean; template Node suite remains 43/43; changed verify passed harness/work-management; full Studio verify passed all 10 domains.
