---
id: T0439
title: Bound Items Workbench I/O and remove obsolete proof coupling
status: doing
project: P001
epic: E016
priority: P2
tags: [items, workbench, performance, cleanup]
created: 2026-07-16
updated: 2026-07-16
---

## What

Remove measured redundant Workbench work and obsolete proof coupling after the
correctness fixes are stable. Preserve fresh-process isolation and keep all
responses bounded; no persistent evaluator or speculative cache.

## Done when

- [x] One canonical focused CLI composition supplies selected-item detail
      (inspect/schema/source/dependencies) from one evaluation, and the server
      coalesces/limits duplicate in-flight catalog requests.
- [x] Initial catalog/detail/chart loading no longer spawns seven concurrent
      evaluator processes; benchmark evidence records command count, wall time,
      context bytes, and process-tree memory before/after.
- [x] Icon preview applies explicit file/section/image caps, avoids synchronous
      full-pack reads, and serves the bounded atlas page without embedding base64
      in every catalog JSON response.
- [ ] Production package code no longer imports hashing from the C-array proof
      exporter; one small neutral hash owner is used by both paths.
- [ ] Feature manifest/docs distinguish production outputs from proof fixtures
      and accurately describe Windows full profiling, cross-platform CI, and the
      separate Linux package proof.
- [ ] Workbench, benchmark, package, feature-contract, and Studio verification
      suites pass with no legacy JSON or second-evaluator path.

## Open questions

Select the smallest concurrency cap from the existing measured fresh-worker
RSS; do not create a long-lived evaluator.

## Log

- 2026-07-16: Created from the full Items architecture review. Runs only after
  T0437/T0438 so optimization cannot obscure correctness failures.
- 2026-07-16: Slice 1 replaced the four-process selected detail fan-out with one bounded `detail` CLI evaluation and added a one-active catalog request coordinator with duplicate coalescing.
- 2026-07-16: Review ACCEPT; semantic parity benchmark reduced initial catalog/detail/chart composition from 7 to 4 commands, 1,084.472 to 946.712 ms, 4,757 to 4,274 stdout bytes, and 277,229,568 to 166,645,760 peak combined process-tree RSS. CLI 23/23, Workbench 16/16, and `studio verify --changed` pass.
- 2026-07-16: Slice 2 removed atlas base64 from catalog JSON. Async bounded pack/header reads now validate asset/atlas/page/region/vertex sections; catalog metadata reads only the capped PNG header and a focused route serves the capped byte-exact PNG. Workbench 35/35 and assets changed-gate pass.
