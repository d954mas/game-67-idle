---
id: T0442
title: template local-mock web shell never hides its loading overlay (game runs fine underneath)
status: review
project: P001
epic: E009
priority: P1
tags: [wasm, web, platform-sdk, template, shell]
created: 2026-07-17
updated: 2026-07-17
---

## What

Any game built on the template + platform-sdk local mock target boots to a
loading overlay stuck at "Running... 45%" forever, while the game itself runs
and renders perfectly underneath (proven by force-hiding the overlay and by
in-page devapi: full scene draws, pack state ready). The 45% is index.html's
own onRuntimeInitialized value; ZERO progress/hide calls from the C side ever
reach JS (pre-load hooks counted only the 4 shell-side calls).

Confirmed defect #1 — `features/platform-sdk/src/platform_sdk.c`
`platform_sdk_game_loading_progress`: the monotonic filter sets
`loading_progress_sent` even when NO backend is installed yet. The template
main calls progress every frame from boot; the web backend installs later
(inside the game-runtime start barrier), so the first post-install call with
an equal value is swallowed forever. Fix candidate: mark sent only when the
value was actually forwarded to a backend.

Open defect #2 — even after forcing `Module._platform_sdk_web_complete_init(1)`
(status -> BOOT_READY), `after_frame_present` never emits
loading_finished/hide on web; the readiness/lifecycle chain needs pinning.

Latent, not a regression: `portal_evidence` releaseLevels() honestly records
local-mock as "unverified — Package verification does not run or record a
local mock simulator session". This browser path was never proven for any
game; it surfaced on the first full headless browser run (2026-07-17, a
private depth-3 game; the full session write-up with capture paths lives in
that game's design/knowledge).

Repro (any template game): `node tools/build_web.mjs && node
tools/serve_web.mjs --preset wasm-release --target local`, open in Chrome.

Introspection method that cracked it (promote to ai_studio/runtime_automation):
raw CDP over node's builtin WebSocket (no puppeteer — Avast-proof): console +
exception capture, pre-load JS hooks via Page.addScriptToEvaluateOnNewDocument,
and in-page engine devapi on a wasm-debug build with GAME_DEVAPI_ENABLED=ON
(stale caches keep NT_DEVAPI_GROUP_* FORCE-OFF — pass CORE/TIME/INPUT/DISCOVERY
explicitly) + a Module setter hook injecting `arguments: ['--devapi', ...]`,
then `window.__devapi.submit(...)`.

## Done when

- [x] Local mock target hides the overlay through the normal C lifecycle
      (progress reaches JS, loading_finished fires, no force-hide needed).
- [x] The platform-sdk progress filter cannot swallow the first real
      forwarded value (regression test in the feature's test suite).
- [x] Defect #2 (hide chain after forced READY) is root-caused and fixed.
- [x] A headless browser check exists (runtime_automation) so local-mock web
      runs are verifiable instead of "unverified" in portal evidence.

## Open questions

- Does a live (non-headless) browser behave identically? (Expected yes — the
  missing calls are platform-independent — but one manual click confirms.)

## Log

- 2026-07-17: found during a game's architecture-review wasm parity pass;
  full CDP probe evidence and scripts in that session's scratchpad; the
  affected game keeps the detailed write-up in its private knowledge base.
- 2026-07-17: Selected as the first P1 in readyQueue after all currentWork tasks reached review. Starting with a browser-shell RED reproduction, then a minimal lifecycle fix and focused template/full verification.
- 2026-07-17: TDD slice 1 RED proved that a pre-backend 45% call poisoned the monotonic filter; the feature now records progress only when a real backend callback receives it. Defect #2 was the template local debug surface replacing the already-installed Emscripten web backend with a native mock that has no loading callbacks; Emscripten now keeps the web backend. Focused C and Node suites pass, wasm-release rebuild passes with warnings-as-errors, and isolated headless Chrome proves the normal C lifecycle reaches 100%, adds `is-hidden`, sets the overlay to `display:none`, and reveals the rendered template scene without a force-hide call.
- 2026-07-17: TDD slice 2 promotes the browser diagnosis into `runtime_automation/web_local_mock_probe.mjs`. Pre-navigation hook instrumentation distinguishes shell progress from the named C-to-JS progress/finished bridges; a passing final DOM without those calls is a RED regression. The raw-CDP probe creates a fresh target, rejects redirects/remote WebSockets/final URLs/network requests, fails closed on missing transcripts or non-release local config, and applies one deadline to target creation, WebSocket/CDP calls, navigation, polling, and cleanup. Unit protocol tests pass 6/6 and the live isolated Chrome observation records shell 0.01/0.02/0.10/0.45 followed by C-bridge 0.10/1.0/finished with no errors. Independent review correctly rejected treating a separately supplied ZIP hash as exact-release evidence, so this slice deliberately emits only a runtime observation; the portal-evidence checkbox remains open until local and packaged builds share a mechanically checked build fingerprint.
- 2026-07-17: PR #22 (`codex/e009-local-mock-overlay`) passed the required GitHub Actions verify jobs on Ubuntu (4m27s) and Windows (4m37s). Slice 3 continued on the stacked `codex/e009-local-mock-release-fingerprint` branch rather than reopening the published probe slice.
- 2026-07-17: TDD slice 3 closes the portal-evidence gap with `runtime-build.json`: the builder hashes game plus canonical engine/feature runtime inputs, rechecks them after the build, passes the source fingerprint into HTML and a web-only compiled C/WASM marker, and refuses source mutation during the build. The raw-CDP probe requires the record before/after navigation, the HTML value, and the marker published by the executing WASM to agree. Package/reopen verification requires the compiled witness in the target-specific WASM; release/manifest/report emission moves to v2 while old v1 packages retain read verification and cannot claim local-mock proof. Local and itch WASM are intentionally different bytes but both final builds carried source fingerprint `475eca96a7aa66feb4f6622389f5de977d45a864c0a9c0d3aeaec573b41bb151`; isolated Chrome returned that compiled marker, the v2 itch ZIP verified it, and portal evidence recorded `local-mock=pass`, `local-sdk-contract=pass`, with public inspector, credentialed smoke, and production certification still honestly `unverified`. Focused Node regression was 57 pass / 1 expected Windows symlink-capability skip; real local+itch Emscripten Release builds passed with warnings-as-errors. Independent re-review found no remaining correctness, security, confinement, compatibility, or coverage findings.
