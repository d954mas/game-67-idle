---
id: T0333
title: "Template web: сборка+паки+сервер из коробки (index.html шелл, пак по HTTP, команда запуска)"
status: doing
project: P001
epic: E009
priority: P1
tags: [template, web, wasm, devapi, packaging]
created: 2026-07-07
updated: 2026-07-07
---

## What

Решение лида 2026-07-07 (важно): шаблон должен уметь собираться под web и
грузить паки БЕЗ ручной настройки, и это должно переезжать в новую игру
из коробки при копировании шаблона.

Контекст: wasm-devapi линк починен (2af7a6cf5, T0328), но живой прогон в
браузере упирается в отсутствие web-упаковки: движок ищет ассет-пак по
вшитому абсолютному file:///-пути, браузер это запрещает — игра не доходит
до установки devapi-shim. Паттерн уже решён для rb-dark
(«web pack streams over HTTP», 5bbbff6f) — перенести в шаблон.

Решение лида: разделение пресетов КАК НА ПК. Человеку — чистые
web-сборки без devapi, релиз И дебаг на выбор (wasm-release +
wasm-debug — зеркало native-release/native-debug); агентам —
wasm-devapi. Все собираются, грузят паки и поднимаются командой
сервера. Внимание: wasm-debug давно не собирался (дерева нет);
санитайзер-фикс 2af7a6cf5 должен покрыть и его линк — проверить.

Скоуп:
1. Web-шелл шаблона: index.html + game.js/game.wasm + пак, отдаваемый по
   HTTP относительным путём (rb-dark паттерн; путь пака в wasm-сборке не
   должен быть абсолютным). Работает для ОБОИХ пресетов.
2. Команда запуска локального сервера — одна команда, которой лид
   корректно запускает web-сборку (правильные MIME для .wasm; смотреть,
   что уже есть у rb-dark/studio_shell — не плодить новый сервер, если
   есть готовый). Умеет оба пресета (человеческий = дефолт для лида,
   агентский — для ботов; разные порты или флаг пресета).
3. Из коробки: games/new_game.mjs копирует шаблон — web-сборка + сервер
   должны работать в новой игре без донастройки и разбирательств.
4. Закрыть хвост T0328: живой shim round-trip — window.__devapi
   submit/poll headless-пробой на wasm-devapi-сборке.

НЕ в скоупе: web-бот как полноценный driver (site+agent parity для
браузерных прогонов) — отдельная карточка, когда дойдём.

## Done when

- [x] ЧЕЛОВЕЧЕСКИЕ пресеты (без devapi), ОБА на выбор как на ПК:
      wasm-release И wasm-debug — одна команда сборки → одна команда
      сервера → лид открывает игру в браузере, пак грузится (без
      file:///-ошибок). wasm-debug линкуется (санитайзер-фикс
      покрывает — проверить первым же прогоном). ФАКТ: wasm-debug
      слинковался с первого прогона; wasm-release лид открыл живьём
      с телефона через nt-app-tunnel — «кубы и кнопка настроек»,
      пак по HTTP 200.
- [x] АГЕНТСКИЙ пресет (wasm-devapi-debug): тот же шелл/сервер +
      headless-проба доказывает window.__devapi.submit/poll round-trip
      (command.describe отвечает). ФАКТ: tests/web_devapi_check.py PASS
      дважды (исполнитель + контрольный прогон): endpoints → 49 команд,
      command.describe{method:endpoints} → 7-полевой дескриптор.
- [x] Пресеты не смешиваются: в человеческой сборке devapi-экспортов и
      shim нет (как на ПК: native-release vs devapi-debug). ФАКТ:
      grep-гейты — 0 devapi-символов в game.js/game.wasm обоих
      человеческих пресетов, 0 `--devapi` в их index.html.
- [x] Новая игра из games/new_game.mjs: web-сборка и сервер работают из
      коробки, ноль ручной настройки. ФАКТ: смоук-копия собралась и
      засервилась без правок (title «Template», MIME верный); следы
      смоука прибраны полностью.
- [x] Хвост T0328 «живой shim» закрыт ссылкой на этот прогон:
      web_devapi_check.py PASS, коммит 613afce55.

## Open questions

РЕШЕНО лидом 2026-07-07 (все вопросы спеки закрыты):

- Q0: вариант А — configure_file подставляет `Module.arguments =
  ['--devapi',...]` ТОЛЬКО в devapi-пресете; один исходник шелла, main.c
  не трогаем; в человеческом index.html подстроки `--devapi` нет
  (негативный гейт).
- Q1: MIME-таблица ИНЛАЙН в serve_web.mjs (~12 строк) — игра
  self-contained из коробки, без зависимости на внутренности ai_studio.
- Q3: ПЕРЕ-РЕШЁН — FATAL-guard НЕ делаем (хак). Вместо него полный вывод
  NT_PRESET_NAME по обеим осям (тип сборки × devapi) на ОБЕИХ платформах:
  Release+devapi получает собственные имена wasm-devapi-release /
  native-devapi-release → клоббер движковых архивов невозможен по
  построению. На ПК была та же латентная мина — закрыта заодно.
- Q6: debug.html вырезан из T0333 → передан T0323 (console-smoke — его дом).

## Log

- 2026-07-07: создана по решению лида после починки wasm-devapi линка
  (расследование T0328: ad-hoc страница не смогла доказать shim из-за
  отсутствия web-упаковки шаблона).
- 2026-07-07: в работу (status=doing). Разведка: rb-dark index.html/debug.html
  живут НЕтрекнутыми в build/wasm-release/bin (ад-хок с джема) — шаблон
  получает трекнутый шелл; пересечение с T0323 (package_web.sh/web-load-smoke/
  scenarios) — спека обязана разграничить скоупы, не дублировать.
- 2026-07-07: ГОТОВО, коммит 613afce55 (спека → 2 ревью → фикс-раунд →
  исполнитель → deep-ревью ACCEPT-WITH-ADDITIONS → контрольный прогон).
  Все done-when отмечены; живой просмотр лида — с телефона через
  nt-app-tunnel. Побочно: web_persistence_check де-клоббер + впервые
  реально прогоняется и FAIL-ит («значение не переживает рестарт
  Chrome») — расследование корня запущено лидом («разбираемся сейчас»),
  advisory, не гейт T0333. Для T0323: package_web.sh должен ВЫЗЫВАТЬ
  tools/build_web.sh (build+copy-pack теперь живут там) + web-load-smoke
  поверх tools/serve_web.mjs; debug.html передан туда же. Закрывает лид.
