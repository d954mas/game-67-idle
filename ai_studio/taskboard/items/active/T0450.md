---
id: T0450
title: "One authoring language: progression tracks move to the shared Lua evaluator"
status: doing
project: P001
priority: P1
tags: [progression, items, lua, balance]
created: 2026-08-10
updated: 2026-08-10
---

## What

Числа игры авторятся двумя способами: предметы на Lua (модули, песочница,
Snapshot), прогрессия на JSON плюс питоновский генератор. Разница
историческая — предметы получили Lua-авторинг в E016, прогрессия осталась на
JSON, и их не свели.

**Переиспользуем вычислитель, а не модель предмета.**

### Трек предметом не является

`ITEM_KEYS` — закрытый список (`items_snapshot.py:41-44`): ни `mode`, ни
`currency_def` там нет, а `section != "level_row"` — жёсткий отказ (`:182`).

Хуже: поле в схеме привязано к «виду» всё-или-ничего — обязательно на каждой
строке подходящего вида, отвергнуто на любом другом (`:249-254`), а
кодогенерация выпускает по структуре на вид. Для 60 треков с разными
колонками это либо один вид, где каждый трек обязан объявить все колонки
игры, либо 60 видов с 240 функциями.

И базы расходятся: прогрессия 0-базная (`cost[level]` — цена ухода с `level`,
`cost_count == max_level`), items 1-базный (строка N и есть уровень N).
Плюс `cost[0]` у прогрессии — приобретение, а в items это отдельное
`acquire.cost`.

### Что делаем

**`studio.tracks` — соседнее пространство объявлений рядом с `studio.items`**,
переиспользующее `studio.levels` и `studio.field` без изменений. Манифест
`items.lua.sandbox.v1` переименовывается в `studio.lua.sandbox.v1`, Snapshot
получает секцию `tracks`.

Прогрессия сохраняет свой генератор, свой 0-базный массив цен, свой словарь
колонок и `progression_value_t`. Меняется только вход: не
`content/progression.json`, а секция снапшота.

Это дополнение к items (**MINOR**), а не ломающая правка: ни `ITEM_KEYS`, ни
версия `items.snapshot.v1`, ни формат пакета, ни ABI не трогаются.

### Что реально переиспользуется

Проверено по коду, не по документам:

| нужно треку | есть в песочнице |
|---|---|
| таблица по уровням | `levels.table` (`items_lua_sandbox.py:237`) |
| линейная кривая | `levels.linear` (`:284`) |
| формульная кривая | `levels.generate` (`:240`) — escape hatch на `studio.math`, **не** готовый `exp` |
| именованные колонки | `levels.columns` (`:300`) |
| объявление колонок | `items.extend_schema({ level_row = … })` (`:174`) |
| многопозиционная стоимость | встроенный `cost_to_reach` + `items.costs({ items.cost(ref, n), … })` (`:162-164`, `:812-819`) |

**`field.cost_list` не существует** — единственный конструктор поля это
`field.i64` (`:187`). Он и не нужен: многопозиционная стоимость уже есть как
`cost_to_reach`. Прежняя редакция цитировала концепт-документ как отгруженный
код.

### Float

Лид: делаем float — фиксированная точка навязывает масштаб каждому будущему
числу, это лишняя бухгалтерия. Степень (`pow`) тоже разрешаем: точности
хватает, расхождение слишком мелкое, чтобы его заметить.

Целочисленность зашита в шести местах: `items_lua_sandbox.py:326-331`
(`checked()`), `:702`/`:736`/`:808` (значения колонок, `linear`, строки
таблицы), `:332-354` (`studio_math` — только `add/sub/mul/idiv/min/max`,
float-арифметики нет вовсе), `items_snapshot.py:186-196,256`,
`items_runtime_package.py:223,478-479,555`.

Форма:

- колонка объявляется дробной; целые остаются целыми;
- во float-формулах разрешены `+ − × ÷` и степень;
- политику округления не заводим. Значение считается **на сборке** и
  запекается C-литералом с точностью, гарантирующей обратное чтение; рантайм
  формул не трогает.

Про `pow` и libm: концепт-документ его запрещал из-за платформенной
зависимости младших битов. Лид принял этот риск, и он гасится конструкцией —
значение считается один раз на машине сборки и коммитится литералом, а
дрифт-гейт байт-сверяет результат. Пересчёт на другой машине покажется как
видимый дифф в ревью, а не как тихое расхождение в рантайме.

`features/progression-core/feature.json:8` обещает «baked int64 curve codegen
(zero float in C)» — строка становится ложной, переписать с бампом.

### Пороговая кривая — дыра, которую нашло ревью

`cost_to_reach` строго про стоимость в предметах. Понятия «накопить xp до
порога» в Lua-конвейере **нет**, а карточка игровой миграции требует ранги треком `threshold`
на дельтах и без этого не стартует. Прежняя редакция несла `advance: { cost |
threshold }` и потеряла порог при переписывании под Lua.

`studio.tracks` обязан выражать оба продвижения — стоимость в предметах
(`manual`/`auto`) и порог собственного xp (`threshold`) — формой, где
представим ровно один вариант на режим.

### Границы

**C приватного потребителя мигрирует здесь, контент — нет.** progression-core компилируется
им in-place (`CMakeLists.txt:29-32`), поэтому удаление
`progression_xp_needed` ломает его немедленно. Его контент переезжает в свою карточку: там `content/progression.json` генерируется и байт-сверяется гейтом.

**Питоновский генератор переживает T0450 и умирает в игровой карточке.** Он —
единственный путь кодогенерации того потребителя (`cmake/GameCodegen.cmake:64-83`);
удалить здесь значит оставить его несобираемым.

### Повторяющиеся поля в событиях — первый слой

Цена уровня становится списком, а событие выразить список не умеет: типы полей
это `bool/int/i64/float/string/hash/bytes` (`state_codegen/schema.py:17`).
Скаляры `cost_def_id`/`cost_amount` держат ровно один предмет, а
`resource_before`/`resource_after` — остаток ровно одной валюты; при цене из
монет и дерева все четыре бессмысленны.

Лид: цену и списание видно в самом `levelup`. Дублирование с событием предметов
принимается осознанно — это разные события, и аналитику проще читать одной
строкой. Строку-рендер («монеты 50, дерево 3») отвергли: нужен настоящий тип, а
не текст, который потом начнут парсить.

Форма — новая секция схемы рядом с `fields`:

```json
"levelup": {
  "fields":   { "track": {"type":"string"}, "old_level": {"type":"i64"}, "new_level": {"type":"i64"} },
  "repeated": { "cost": { "def_id": {"type":"string"}, "amount": {"type":"i64"},
                          "before": {"type":"i64"} } }
}
```

`after` в записи НЕ нужен: это `before - amount`.

Транспорт уже есть — строки и `bytes` докладываются инлайн после структуры по
смещению от её начала (`render_events.py:57,75,140-155`). Генератор печёт:
`uint32_t cost; uint32_t cost_count;` в структуре, POD-строку записи, доступ
`..._cost_count(e)` / `..._cost_at(e, i)`, и запись в таблице дескрипторов,
чтобы devapi-хвост и аналитика печатали список сами
(`render_event_descriptor`). Строки внутри записей — те же смещения от начала
события.

Одна валюта — список из одной записи; ветки «а если одна» в коде не заводим.

Этот слой не про Lua и не про треки, но лежит здесь, а не отдельной карточкой:
иначе `levelup` пишется дважды. Он же чинит `items.payment`, который сегодня не
показывает состав НИ ОДНОЙ покупки — только отпечатки и суммы
(`state/items.schema.json`, событие `payment`). `items.payment` переезжает на
`repeated` в этом же заходе.

### Удаление xp_needed

`progression_xp_needed` возвращает одно число там, где вопрос стал
списочным. Удаляется вместе с `progression_can_level_up`, построенным поверх
него (`progression.c:207`).

Десять вызовов в семи файлах. Шаблон: `src/ui/demo_hud.c:26`,
`tests/test_progression.c:167,169`, `tests/test_template_composition.c:290`.
Приватный потребитель: `src/gameplay/eff_fold.c:127` (это `can_level_up`, и
это НЕ `eff_registry.c` — T0118 расщепил его), `src/ui/trolley_ladder_modal.c:188`,
`src/ui/upgrades_tree_view.c:226`, `tests/test_progression.c:167,169`,
`tests/test_template_composition.c:218`. Плюс четыре комментария, называющих
удаляемое: `src/ui/upgrades_panel_model.h:10,21`, `src/ui/upgrades_tree_view.c:6,210`.
Сам модуль: `progression.h:41-42`, `progression.c:187,199,207`.
Показываем пока одну позицию, но через `cost_count`/`cost_at`.

### API

```c
progression_track_ref_t progression_track(const char *id);   /* резолв один раз */
float    progression_valuef(progression_track_ref_t, progression_value_t);
float    progression_valuef_at(progression_track_ref_t, progression_value_t, int level);
int64_t  progression_valuei(progression_track_ref_t, progression_value_t);
int64_t  progression_valuei_at(progression_track_ref_t, progression_value_t, int level);
uint32_t progression_cost_count(progression_track_ref_t);
```

`_at` нужен карточке апгрейда для «1.6 -> 1.8» без арифметики в UI. Хэндл
вместо строки убирает линейный `strcmp`-поиск (`progression.c:138-148`).

Несовпадение типа колонки и вызова — **рантайм**, а не сборка: обе функции
берут один `progression_value_t`. Либо различимые типы на класс колонки, либо
не обещать проверку на сборке. Прежняя редакция обещала — снято.

### Законы в README

**Трек хранит только свой вклад; база живёт там, где живёт то, на что
влияют.** Проверка: население в живой игре трогают шесть узлов, своя база у
каждого была бы шестикратной ерундой.

Items **заставляет** авторить строку 1 — значит явно сказать, что она несёт
нулевой вклад, иначе авторы положат туда базу.

### Миграция шаблона

| файл | почему |
|---|---|
| `content/progression.json` | исчезает |
| `state/progression.schema.json:19-34` | событие `levelup` несёт скалярные `cost_def_id`/`cost_amount`/`resource_before`/`resource_after`/`cascade_depth` — форма решена ниже, см. «Повторяющиеся поля». Сеалятся только `fields`/`types`/`enums`, события не запечатаны, а у шаблона лока нет вовсе — `state_lock.mjs` живёт только у второго потребителя |
| `tests/test_progression_catalog.c` | `casc_a`/`casc_b` умирают с `to_track`; `runaway` — та форма, которую выносит новый гейт |
| `tests/test_progression.c`, `test_progression_curve.c` | пинят `currency_def`, `on_level_up`, `cost[i]`, `xp_needed`. **Оба файла есть и в шаблоне, и у второго потребителя** — править обе копии |
| `src/ui/demo_hud.c:22-25` | `xp_needed` каждый кадр |
| `templates/template/game-dependencies.json` | `validate_contracts.mjs:122-123` требует совпадения версий. У второго потребителя файл зовётся `dependencies.json` и валидатором не проверяется |
| `ai_studio/dev_environment/python_contracts.test.mjs:21` | пинит путь генератора |
| `ai_studio/dev_environment/cmake_split.test.mjs:12` | пинит цель `progression_tracks_gen` |

### Выдача при достижении уровня

Лид: в первом заходе целиком, включая рантайм. Сегодня `on_level_up`
**запрещён генератором** (`generate_progression_tracks.py:210`), и
`apply_on_level_up` в реальной сборке никогда не исполнялся — значит покрыть
тестами через `templates/template/tests/test_progression_catalog.c`.

Вместе с ним: гейт против `auto`-трека, выдающего на любом уровне ≥
собственной цены того же предмета (иначе бесконечный цикл, ради которого
стоит `PROGRESSION_MAX_LEVELUPS_PER_TRACK` — эта защита ОСТАЁТСЯ).

**Каскады удаляются.** Лид: механика не нужна, её назначение не
восстанавливается. Уходят поле `progression_emit_t.to_track`
(`progression.h:18`), ветка рекурсии (`progression.c:396-398`),
`PROGRESSION_MAX_CASCADE_DEPTH`, глагол `"convert:cascade"`, поле события
`cascade_depth` и тесты `casc_a`/`casc_b`. Рантайм для них написан, но
генератор запрещает объявлять `on_level_up`, поэтому каскад не исполнялся
ни разу ни в одной сборке; единственное объявление во всём репозитории —
рукописный `test_progression_catalog.c:49-55`, написанный ради доказательства
защиты от рекурсии. В `progression_emit_t` остаётся `{def_id, amount}` —
выдача предметов.

Записать: `progression_set_level` (`:271-291`) выдачу **не** прогоняет, прыжок
0→5 пропускает пять уровней; DevAPI и тесты ходят именно через него.

Глагол `"loot:levelup"` (`:392-394`) — причина **выдачи** предметов, а не
списания (это `items_try_stack_add`); прежняя редакция называла его неверно.
Решение: фиксируем контрактом модуля и записываем в `INSTALL.md`, что игра
обязана иметь глагол `loot`. Он уже есть в обоих словарях, а действие
описывает модуль, а не политику игры. Списание уровня идёт под
`level_cost:<track_id>` — этот глагол тоже есть у обоих.

## Done when

- [ ] `studio.tracks` объявляется тем же вычислителем; `studio.levels` и
      `studio.field` не изменены; Snapshot получает секцию `tracks`
- [ ] Выразимы оба продвижения: стоимость в предметах и порог xp; представим
      ровно один вариант на режим
- [ ] Дробные колонки: дробный тип поля, float-арифметика включая степень,
      запекание с гарантией обратного чтения
- [ ] `repeated`-секция в схеме событий: генератор, доступ по индексу,
      печать в devapi-хвосте и аналитике, тесты генератора
- [ ] Многовалютная стоимость платится атомарно через `items_try_pay_stacks`,
      и порядок именно «`items_can_pay_stacks` → аллокация трека → оплата»
      (заплатить первым сжигает кошелёк при исчерпании слотов треков — ровно
      тот дата-лосс, от которого закалён `progression_level_up`)
- [ ] Выдача при достижении уровня работает в рантайме и покрыта тестами
- [ ] Гейт против самоокупаемого `auto`-трека
- [ ] Каскады удалены: `to_track`, рекурсия, `PROGRESSION_MAX_CASCADE_DEPTH`,
      `convert:cascade`, `cascade_depth`, тесты `casc_a`/`casc_b`
- [ ] `progression_xp_needed` и `can_level_up` удалены; все десять вызовов и
      четыре комментария переведены на `cost_count`/`cost_at`
- [ ] `levelup` несёт `cost[] = {def_id, amount, before}`; пять скалярных полей
      цены удалены; `items.payment` переведён на ту же форму
- [ ] Законы в README: «трек хранит только вклад», «строка 1 — нулевой вклад»,
      «`set_level` не выдаёт гранты»
- [ ] Питоновский генератор жив, читает снапшот; приватного потребителя собирается на всех шагах
- [ ] Версии бампнуты (game-state MINOR, items MINOR, progression MAJOR), `## Compatibility`
      обновлён, `feature.json` прогрессии не обещает «zero float in C»,
      `game-dependencies.json` шаблона синхронизирован
- [ ] `validate_contracts.mjs`, `python_contracts.test.mjs`,
      `cmake_split.test.mjs` зелёные

## Blocked by

T0449 (атомарная оплата), T0448 (переписывает путь генерации C у items и
трогает тот же код заголовков), T0447.

## Log

- 2026-08-10: выделено из T0446, затем переписано после ревью. Первая
  редакция опиралась на таблицу, собранную по концепт-документу:
  `field.cost_list` не существует, `levels.generate` не готовый `exp`. Модель
  предмета треку не подходит — отсюда `studio.tracks` вместо «трек это
  предмет». Пороговая кривая была потеряна при переписывании и возвращена:
  без неё игровая миграция не стартует.
- 2026-08-10: Блокеры закрыты (T0447, T0448, T0449) — карточка на фронтире.
  Решения лида по трём открытым вопросам внесены в тело: каскады удаляются;
  `levelup` несёт цену сам, через новую `repeated`-секцию схемы
  `cost[] = {def_id, amount, before}` (без `after` — это вычитание), и этот
  слой заводится ЗДЕСЬ, не отдельной карточкой; глаголы причин фиксируются
  контрактом модуля в `INSTALL.md`. Исправлены фактические ошибки прежней
  редакции: `loot:levelup` — причина выдачи, а не списания; мест под
  `xp_needed`/`can_level_up` не девять, а десять вызовов в семи файлах плюс
  четыре комментария, и `eff_registry.c:184` больше не существует (после T0118
  это `eff_fold.c:127`, и там `can_level_up`).

  Что приехало из T0449 и на что опирается эта карточка:
  `items_try_pay_stacks(scope, def_ids, counts, count, reason)` — атомарная
  оплата списком на всегда-доступном API (только стековые ресурсы, не более
  `ITEMS_PAYMENT_MAX_REQUIREMENTS`=16 позиций, повтор предмета запрещён,
  count 0 = бесплатная позиция и выбрасывается, пустой список = успех, область
  обязана быть валидной в любом случае, отпечатки считаются в объявленном
  порядке); `items_can_pay_stacks(scope, def_ids, counts, count)` — тот же
  планировщик без коммита, для порядка «проверить → аллоцировать → заплатить».
  items-core 4.1.0.

  Чего в API НЕТ намеренно: `ITEMS_RESULT_INSUFFICIENT` не сообщает, чего не
  хватило. Дефицит считается вызывающим — сумма `items_stack_count` по
  контейнерам области минус требование. Расширять API под это не надо.

  Ещё одно изменение вне карточки, которое надо знать: проверок «влезет ли
  событие в лог» больше не существует. `game_event_can_emit` удалён из
  game-events (2.0.0), `ITEMS_RESULT_AUDIT_UNAVAILABLE` и все
  `audit_*_available` — из items-core, батч-проверка сида — из шаблона.
  Мутации коммитятся и пишут событие; переполнение кадрового лога это ошибка
  разработчика, которая ассертит в дебаге и дропает со счётчиком в релизе.
  Писать такие проверки заново не надо.

  Долг по комментариям на снос вместе с переписываемыми строками
  (`progression.c`): `L-fix (deep-review #N)` на :150-156 и :354-357,
  `H-fix (deep-review #1, data-loss)` на :226-231, `Cut A` на :243 и :385-387,
  `#17` на :374, смесь языков на :376. Правило репозитория — комментарий
  объясняет ПОЧЕМУ, без номеров ревью, задач и истории.
- 2026-08-10: Слой 1 (repeated в схеме событий) отгружен: студия 004cdc6e0, игра 34ff5b1. Форма секции = map имя -> map член; запись несёт скаляры и строки, bytes отвергнут (пер-запись длина без потребителя). Записи поехали ОТДЕЛЬНЫМ семейством дескриптора (game_event_record_t + records/record_count на game_event_desc_t), а не пятым членом game_event_field_t: рукописные таблицы полей не тронуты, а вложенная repeated внутри записи стала невыразимой, а не просто запрещённой. Двенадцать рукописных game_event_desc_t дописаны двумя членами (-Wextra -Werror отвергает частичный инициализатор). Строки записей хранят смещение от начала события, поэтому retain слепым memcpy читается. Пустой список = легальная нулевая цена. Фикстура mini получила событие paid (запись выравнена строже структуры события -> прокладка и расширение alignment у game_event_emit); голдены перегенерены. Шаблон 48/48, игра 107/107, validate_contracts зелёный. game-state 4.1.0, game-events 2.1.0.

Решение лида по пороговым трекам: предметная цена едет в cost[] = {def_id, amount, before}, опытная — в xp_cost + xp_before (after не нужен, это before минус cost). Основание: снятые resource_before/after описывали остаток РОВНО одной валюты и при цене «монеты + дерево» бессмысленны, а собственный xp у трека всегда один (TrackState { level, xp }).

Расхождения карточки с кодом, найденные при разборе:
1. items_runtime_package.py больше не существует (снесён в T0448 вместе с блобом). Целочисленность items сегодня зашита в items_c_catalog.py (_i64 на :68-70, значения строк :317, литералы INT64_C :525), плюс песочница и снапшот — не шесть мест, а пять.
2. Версия game-events в карточке не перечислена, а дескриптор и рендер живут в ней. Бампнута 2.0.0 -> 2.1.0.
3. Вход генератора прогрессии против границы «контент приватного потребителя не мигрирует»: у него 60 треков в content/progression.json, а секции tracks в его снапшоте не будет. Значит на время T0450 генератор обязан принимать ровно один из двух входов, и второй умирает в игровой карточке.

Ещё: приватная игра пинит game-state коммитом (git archive в build/baseline), а game-events читает живьём — правка заголовка без перепина даёт ABI-химеру. Перепин сделан. В рабочем дереве игры лежит чужая незакоммиченная работа по people_v3 — не тронута.
- 2026-08-10: Слой 2 отгружен: студия 7aa588fa8, игра b0843e3. levelup — cost[] {def_id, amount, before} для manual/auto (одна запись сегодня, форма уже списочная), xp_cost + xp_before для threshold, cost[] пуст; пять скаляров (cost_def_id, cost_amount, resource_before, resource_after, cascade_depth) удалены. Кап глубины каскада пока живёт, снимается вместе с каскадами. items.payment получил ту же форму cost[], before читается по ВСЕЙ области оплаты до применения строк; его собственные скаляры (requirement_count, requested_units, applied_units, scope_count, source_entry_count, три отпечатка) оставлены — счётчик и сумма описывают список честно, в отличие от идентичности одного предмета, и снятие только у payment рассинхронизировало бы его с acquire/upgrade. Побочно: ITEMS_PAYMENT_MAX_REQUIREMENTS теперь ограничивает и запечённую каталожную цену, а не только авторский список — иначе аудит нельзя выписать целиком; более длинная цена отвергается ITEMS_RESULT_INVALID_ARGUMENT. Тесты: levelup_paid_event_exists / levelup_threshold_event_exists, состав платежа пинится в обоих аудит-тестах шаблона и в игровом тесте своей валюты (там же зафиксировано, что stacks-оплата отчитывается в ОБЪЯВЛЕННОМ порядке, а каталожная — в отсортированном). Шаблон 48/48, игра 107/107. items-core 4.2.0, progression-core 4.0.0.

Замечено, не тронуто: в levelup поля mode и cause всегда несут одну и ту же строку (mode_name(def->mode) и литерал в вызове совпадают во всех трёх режимах). Дубликат вне карточки — жду решения.
- 2026-08-10: Слой 3 отгружен: студия 2610379d9, игра cd90109. studio.tracks — соседнее пространство объявлений на том же вычислителе: id (голый слаг), kind (колонки биндятся через тот же реестр required_for, что и у предметов), mode, levels. Строка 1 = неулучшенное состояние с нулевым вкладом, достижимые уровни = строки выше неё. Ровно один вариант продвижения на режим: cost_to_reach у manual/auto, xp_to_reach у threshold; объявление чужого падает там, где написано. Разбор уровней вынесен в ОДНУ функцию на оба пространства, различается только построчный переход.

Дробные колонки: field.f64. Точная колонка обязана объявить rounding, дробная не может — значение считается один раз на машине сборки и запекается с гарантией обратного чтения, рантайм-округления нет, значит и политики нет. studio.math следует за колонкой: add/sub/mul/min/max берут два операнда ОДНОГО вида и возвращают его же, idiv только точный, div и pow только дробные, tofloat — единственное место, где точность кончается. Смешанные операнды = ошибка авторства, а не промоушен. Каждый дробный результат проверяется на конечность, поэтому переполнение в бесконечность и отрицательное основание под дробной степенью падают на сборке. levels.linear принимает дробные start/step (оба одного вида).

Snapshot получил секцию tracks рядом с items, ВНУТРИ content_hash. C-каталог предметов её игнорирует и игнорирует любое поле, которого не требует ни один вид предмета — эти колонки принадлежат соседнему пространству и имеют свой генератор.

Расхождение с карточкой по версии: манифест переименован в studio.lua.sandbox.v1, это правка одной строки у КАЖДОГО потребителя, плюс сдвиг content_hash каждого снапшота. По определению SemVer в README это MAJOR, а не MINOR — items-core 4.2.0 -> 5.0.0. items.snapshot.v1, C-каталог и ABI не тронуты.

Удалено: features/items-core/tests/fixtures/items_runtime_snapshot_v1.json — на него не ссылался никто, он остался от снесённого в T0448 рантайм-блоба.

Проверка: питон-пояс items 4 модуля зелёные (24+19+20+13), шаблон 48/48, игра 107/107, validate_contracts и python_contracts зелёные.

ВНИМАНИЕ ЛИДУ: сборка игры переписала assets/packs/game-runtime-generated/assets.jsonl и оставила последнюю строку НЕВАЛИДНЫМ JSON (tram_v2_asset_contract упал на разборе). Файл восстановлен из HEAD, тесты снова зелёные, но воспроизведётся при следующей сборке — это генератор паков на фоне незакоммиченной работы people_v3, не моя правка.
- 2026-08-10: Слой 4+5 отгружен: студия 1145a4ba2, игра 79ec9f8.

Авторинг: content/progression.json удалён в шаблоне и в игре, флаг --catalog вместе с ним. Генератор читает только секцию tracks снапшота. Шаблонный hero переписан формулой (levels.generate, целочисленный числитель/знаменатель) — 20 цен совпали с прежними побайтно. 60 треков игры перенесены транспортом: те же id (id = ключ записи в сейве, переименование забыло бы уровни), те же числа, режимы; каждая запечённая цена пересобрана из Lua и сверена с JSON поэлементно ДО удаления файла.

Рантайм: progression_step_t на уровень — цена списком предметов ЛИБО одно xp-пороговое число; оплата атомарная и в порядке проверить/аллоцировать/заплатить. Выдача при достижении уровня отгружена целиком, включая рантайм (раньше кодоген её запрещал и путь не исполнялся ни разу). Колонки: exact/fractional таблицы на трек, чтение через valuei/valuef, тип колонки решает какое чтение отвечает (несовпадение = ассерт в дебаге и ноль). Все вызовы берут progression_track_ref_t; дерево апгрейдов опрашивает 59 узлов за кадр и теперь резолвит их один раз в статическую таблицу.

Снято: xp_needed (одно число там, где цена стала списком) и can_level_up поверх него — доступность считает items_can_pay_stacks по списку шага, дефицит считает вызывающий; каскады целиком (to_track, рекурсия, кап глубины, поле cascade_depth); cause из levelup. Добавлено progression_payment_scope() — чтобы вызывающий спросил items о доступности, не лезя в контейнеры игры (иначе eff_fold тянул бы game_items в тест-таргет).

Гейт самоокупаемости стоит в ДВУХ местах: в песочнице и на границе снапшота — auto-трек, возвращающий на уровне >= собственной цены того же предмета, отвергается. Кап 64 лвлапа на трек за кадр остался как страховка от почти-самоокупаемой кривой.

Версии: items-core 5.1.0 (grants, точечные id треков), progression-core 5.0.0. Законы в README прогрессии записаны (вклад без базы, строка 1 = нулевой вклад, set_level не выдаёт гранты).

Проверка: шаблон 48/48 зелёный. Питон-пояс items зелёный. validate_contracts, python_contracts зелёные; cmake_split падает на предсуществующем санитайзер-тесте (проверено через stash ещё в слое 1).

БЛОКЕР НЕ МОЙ: полную сборку игры собрать нельзя — в её дереве идёт параллельная незакоммиченная работа people_v3, и она сейчас не компилируется: src/render/walker_accessory.h объявляет WALKER_ACCESSORY_RICH_BROKER/RARE_CELEBRITY/VIP_TYCOON, а src/render/walker_render.c и src/scene/scene_bootstrap.c ждут WALKER_ACCESSORY_TIE/BRIEFCASE/TOP_HAT (9 ошибок, только в этих двух файлах; заголовок правился в 23:12 по ходу моей сессии). Все прогрессионные цели игры собраны и прогнаны отдельно: test_progression, test_progression_curve (все 60 треков на новой форме), test_eff_registry, test_template_composition, test_items_fragment, test_upgrades_panel_model, test_trolley_ladder_layout, test_upgrades_tree_layout — 8/8 зелёные. Полный ctest игры прогнать после того, как people_v3 соберётся.
