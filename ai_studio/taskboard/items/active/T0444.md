---
id: T0444
title: Harden Items runtime transactions for assert-off production builds
status: review
project: P001
epic: E019
priority: P0
tags: [items, runtime, transactions, regression]
created: 2026-07-18
updated: 2026-07-18
---

## What

Close the post-merge Items Runtime v2 correctness gaps that are hidden when
`NT_ASSERT_MODE=0`: payment fingerprint construction must not depend on assert
side effects, public transaction inputs must fail safely, partial same-container
moves must preserve unique slots, and bound catalog costs must remain valid for
runtime payment semantics.

## Done when

- [x] Assert-off regression coverage proves paid payment/acquire/upgrade initialize
      every catalog ref used by transaction fingerprints.
- [x] Partial same-container AUTO moves either produce one valid split slot or
      return a normal refusal without mutating state.
- [x] Invalid/oversized payment scopes, stale refs, non-stackable cost resources,
      and overflowing cost totals fail before state mutation or audit emission.
- [ ] Focused Items runtime/package tests, the full mandatory verify, and
      Ubuntu/Windows CI pass with QTECH evidence recorded.

## Open questions

- 2026-07-18: Select existing `items_result_t` refusal codes for invalid public
  refs and malformed runtime costs while preserving the current API surface;
  resolve from established neighboring behavior during the first TDD slice.

## Log

- 2026-07-18: Started from post-merge review of E019. First slice is the
  `NT_ASSERT_MODE=0` payment fingerprint release-blocker; unrelated Workbench,
  release-tooling, Taskboard, and Canvas findings remain outside this task.
- 2026-07-18: Slice 1 RED: assert-off target produced three deterministic
  fingerprint failures in payment/acquire/upgrade. GREEN: moved catalog lookup
  outside `NT_ASSERT` with release-visible refusal; normal and assert-off
  fragment suites pass (35 tests each).
- 2026-07-18: Slice 2 RED: assert-off persistent partial self-move placed both entries in slot 0. GREEN: partial moves no longer ignore their source slot; explicit source-slot split refuses atomically and AUTO selects slot 1. Normal and assert-off fragment suites pass (36 tests each).
- 2026-07-18: Slice 3 RED: zero payment scope returned INSUFFICIENT in assert-off instead of rejecting the caller contract. GREEN: zero/oversized/duplicate scopes return INVALID_ARGUMENT, stale persistent refs return NOT_FOUND, and ephemeral scopes return LIFETIME before indexing; normal and assert-off suites pass (38/37 tests).
- 2026-07-18: Slice 4 RED: assert-off paid acquire accepted a stale destination generation and reached later validation. GREEN: acquire, upgrade, and persistent move now validate generation/index/used before dereference and return NOT_FOUND without mutation; null acquire output returns INVALID_ARGUMENT. Normal and assert-off suites pass (39/38 tests).
- 2026-07-18: Slice 5 RED: a resigned package whose cost targeted a unique item bound successfully. GREEN: every acquire/level cost span rejects unique resources, duplicate resources, and i64 unit-sum overflow before catalog publication; runtime package plus normal/assert-off fragment suites pass.
- 2026-07-18: Integration gate: first full verify exposed missing sanitizer ownership for the new assert-off executable; harness contract updated. Re-run passed all 10 domains in 58.3s. Awaiting Ubuntu/Windows PR CI before closeout.
