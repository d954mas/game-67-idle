# Tech

Owns runtime-facing technical pipeline work:

- game template integration;
- runtime automation;
- DevAPI tooling;
- state codegen;
- engine-boundary adapters;
- per-game technical implementation conventions.

Current source material lives in `template/`, `tools/devapi/`,
`tools/state_codegen/`, and `external/neotolis-engine/` references.

