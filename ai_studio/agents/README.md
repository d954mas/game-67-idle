# Agents

Owns the agent model for the studio pipeline:

- lead agent responsibilities;
- worker/subagent boundaries;
- packet presets;
- handoff rules;
- review and verification authority.

Current source material lives in `docs/ai-pipeline/agent-workflow.md`,
`docs/ai-pipeline/subagent-protocol.md`, and taskboard packet templates.

