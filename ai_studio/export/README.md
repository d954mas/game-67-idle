# Export

Owns portability and generated harness surfaces:

- portable pipeline export;
- generated Claude skill pointers;
- generated hook config;
- compatibility surfaces for other harnesses.

Current source material lives in `tools/bootstrap/`, `tools/sync.mjs`,
`tools/skills_sync.mjs`, and `tools/hooks_sync.mjs`.

