# Asset Prep Library

Small Python helpers shared by asset preparation tools.

This group is not a public workflow surface. Use it for reusable, low-level
helpers that support `prep/` commands, such as atomic writes. Domain decisions
belong in the caller module.

