# Чек-лист проверки (для лида) — вечер 2026-07-02 (+ночь 2026-07-03)

## УТРО: сводка ночного прогона (2026-07-03)

За ночь принято 6 задач, все на живом сервере, тесты 204→240, все ворота
зелёные на каждом шаге: T0204 панель истории (клавиша `), T0202 тёплый
python-воркер (детект/рендер ×13-22 быстрее), T0226 скилл для агентов
(смоук пройден), T0228 🤖 у агентских шагов в истории, T0229 экспорт
переделан (диалог с именем + zip, суффикс удалён), T0210 АЛЬФА на канвасе
(инкремент 1). Секции ниже — с пометкой ночи. Начни с Ctrl+Shift+R.

## АЛЬФА на канвасе (T0210, ночь — ГЛАВНОЕ к утру)

- [x] В benchmark-fixture-c7f9dc рядом с крыльями (x=1400) лежит ДУБЛИКАТ,
      прогнанный через альфу (метод matte). ВЕРДИКТ ЛИДА: «крылья ок»
      (2026-07-03). T0210 закрыт.
- [x] Выбери любую картинку → инспектор, секция Regions → Alpha — проверено
      живьём в ходе утренней сессии (3 бага найдено и исправлено:
      трейсбек → чистый отказ, фиолетовый ареол, воскрешение скрытых
      пикселей). Массовая альфа на мультивыделении (T0230) тоже принята.
- [x] Выдели регионы → альфа применяется только внутри них. (лид: «ок»,
      2026-07-03 — альфа-секция закрыта целиком)
- [x] Auto на «мягком» арте честно отказывает — проверено лидом живьём
      (сначала показывал сырой трейсбек, исправлено на понятное сообщение).

## Живые фиксы 2026-07-03 (по твоим репортам — перезагрузи страницу)

Всё уже на живом сервере :8780. Сервер ПЕРЕЗАПУЩЕН после приземления текста
(T0222) — жёсткое обновление (Ctrl+Shift+R) теперь ОБЯЗАТЕЛЬНО на всех
страницах: новый бэкенд канваса, шрифты, и активирован фикс галереи (T0225).

## Сайт студии (T0220 зачистка)

- [x] Канвас несёт СТАНДАРТНОЕ левое меню студии (то же, что на других
      страницах, сворачивается кнопкой ‹, Canvas подсвечен). Верхней полосы нет.
- [x] Главная студии: карточки идут В ПОРЯДКЕ ЛЕВОГО МЕНЮ (решение лида при
      проверке, 7a92f3ce); пункт Canvas есть в меню всех страниц
      (главная/таскборд/карта/quality/галерея).
- [x] Галерея ассетов `/asset_viewer/` — НАЙДЕН РЕГРЕСС: пропали красивые
      превью (показывается генерация вместо превью) → задача T0225.
      Остальное работает; /asset_prep нет — ожидаемо.

## Канвас — исправления из живого тестирования

- [x] Создание региона (рект И полигон) → сразу имя «<имя элемента> N»,
      а не размеры. В списке регионов размеры только в тултипе.
- [x] (найдено при проверке) Меню группы раздулось → урезано на месте
      (72eab3ab): Order/Ungroup/Copy ID/Delete; Move to group удалён везде.
- [x] Shift-клик в layers выделяет ДИАПАЗОН от последнего обычного клика
      (как в Figma); Ctrl-клик — поштучно. (в списках РЕГИОНОВ диапазона
      нет — записано в T0224)
- [x] Отступ элементов внутри группы — ужат до 8px/уровень (deefe896,
      проверено скриншотом). Глянь ещё раз ПОСЛЕ Ctrl+Shift+R — прошлый раз
      у тебя был закэшированный CSS.
- [x] Copy ID в правом клике — ПРОВЕРЕНО в чате: ты кинул 3 canvas:// ссылки,
      я разрешил их через CLI (петля Copy ID → агент работает).
- [x] Slice → все кропы падают в новую группу «<лист> slices»; ОДИН Ctrl+Z
      убирает группу вместе с кропами. (подтверждено лидом ранее, живая сессия)
- [x] Delete группы удаляет и содержимое (undo вернёт всё разом) —
      подтверждено лидом (canvas+layers). Ungroup — проверяется ниже в
      секции «Целостность» (z-слот).
- [x] Подменю Order не закрывается по дороге курсора. (лид: «работает»,
      2026-07-03)
- [x] УСТАРЕЛО: хинт «Coming soon» заменён настоящей секцией Alpha (T0210
      приземлился и принят).

## ЭКСПОРТ ПЕРЕДЕЛАН (T0229, ночь 2026-07-03 — по твоим трём замечаниям)

- [x] Экспорт ОДНОЙ картинки → save-диалог с редактируемым именем.
      (лид: «экспорт работает. Картинка и zip», 2026-07-03 — T0229 закрыт)
- [x] Экспорт НЕСКОЛЬКИХ (или всего проекта) → один ZIP. (лид: ок)
- [x] Суффиксы `name@2x.png` — авто-нейминг покрыт тестами; проверится
      вживую вместе с масштабами в секции T0206 ниже.
- [x] Отмена в диалоге = тихая отмена — покрыто тестом/кодом; отдельной
      живой проверки не требует.

## Экспорт-панель (T0206) — главное новое (частично УСТАРЕЛО: пункты про
   папку заменены секцией T0229 выше; проверяй строки/масштабы/качество)

- [ ] Внизу инспектора секция Export: строки «масштаб + суффикс + формат»,
      «+» добавляет, «×» убирает. Настройки переживают перезагрузку страницы
      и откатываются Ctrl+Z.
- [ ] Для JPG/WebP появляется ползунок качества; переключатель
      Lanczos / nearest.
- [ ] Кастомный масштаб: ввести `512w` → экспорт шириной 512 с сохранением
      пропорций; `2x` → вдвое больше.
- [x] УСТАРЕЛО: «папка как в Figma» заменена save-диалогом T0229 (принят
      выше) — выбора папки больше нет вовсе.
- [ ] Без выделения кнопка = «Export project (N screens)» — экспортирует все
      видимые группы.
- [ ] Export из контекстного меню исчез (теперь только панель) — ожидаемо.
- [ ] СУПЕРСЕМПЛИНГ (последний пункт приёмки T0206): взять/сгенерить лист в
      2x от целевого размера → экспорт 0.5x (или 512w) с Lanczos → сравнить
      шум с оригиналом. Ожидание: заметно чище. Результат (да/нет + пара слов)
      скинуть мне — допишу в лог задачи.

## Перф-пасс (T0200) — приземлился ночью, после экспорта

- [ ] Канвас ощутимо отзывчивее: после move/rename/delete нет паузы на
      перезагрузку (страница живёт на ответе операции, лишних запросов нет).
- [ ] Превью в layers не перемигиваются при каждом действии (картинки
      кэшируются и переиспользуются).
- [ ] Перетаскивание (пан, элементы, регионы) плавное даже на большом листе.
      ЛИД 2026-07-03: НЕ гладко, «шагами»; плюс зум колесом посреди драга
      ломает привязку к курсору → T0236 (диагностика+фикс в работе).
- [ ] ГЛАВНОЕ: выделить рамкой несколько элементов → подвинуть → ОДИН Ctrl+Z
      возвращает всё; то же с множественным удалением (раньше Ctrl+Z шагал
      по одному элементу).

## Тосты вместо статус-строки (T0203)

- [ ] Нижней статус-строки больше нет; подтверждения/ошибки приходят
      тостами справа внизу (успех тает ~3 с, наведение задерживает;
      ошибки висят до ×).
- [ ] Detect/Slice/Render/Export показывают тост-прогресс со спиннером,
      кнопка на время операции гаснет, канвас НЕ блокируется.
- [ ] Запусти 3 длинные операции подряд: третья видимо стоит в очереди
      («Queued … #2») и стартует, когда освободится слот; из очереди можно
      снять крестиком. Результат экспорта — закреплённый тост со ссылками.
- [ ] Undo/redo тостов не показывают — так задумано (не шум).

## Группы v2, инкремент 4 (T0219, ФИНАЛ) — клип по границам

- [ ] У группы в инспекторе галка «Clip content» (рядом с Visible): вкл —
      всё торчащее за рамку обрезается на канвасе; выкл — как раньше.
      Правый клик по группе — тот же переключатель.
- [ ] Обрезанный кусок НЕ кликается на канвасе, но: выбери элемент в layers —
      его спрятанная часть подсветится призраком (0.25 альфы), спрайт не
      «потерян». Toggle off возвращает всё как было, Ctrl+Z работает.
      (по твоему решению призрак станет СКРЫТ по умолчанию с показом по
      Alt — T0224; пока ещё виден всегда)
- [ ] Вложенные клипы пересекаются (клип в клипе режет по обеим рамкам).
- [ ] Render group / экспорт: торчащее за ВЛОЖЕННУЮ клипающую группу
      обрезано в PNG; границы самого экрана как раньше.

## Группы v2, инкремент 3 (T0219) — ВЛОЖЕННОСТЬ + выбор как в Figma

- [ ] ГЛАВНОЕ (твой кейс с кнопкой): клик по спрайту внутри группы выбирает
      ВСЮ группу — составная кнопка тащится сразу целиком, со всем вложенным.
- [ ] Дабл-клик проваливается на уровень глубже (экран → кнопка → спрайт);
      дабл-клик на спрайте = регионы, как раньше. Ctrl+клик = сразу спрайт.
- [ ] Esc поднимается на уровень вверх; клик по пустому месту — выход в
      корень. Хлебная крошка показывает, где ты.
- [ ] Группу можно вложить в группу: в layers перетащить строку группы НА
      заголовок другой (середина строки = внутрь, край = поменять порядок).
      В свою же вложенную — нельзя (цикл, цель неактивна).
- [ ] Перемещение группы двигает всё вложенное; глаз на родителе прячет всё
      поддерево; Delete группы удаляет всё поддерево (один Ctrl+Z вернёт);
      Ungroup разбирает только один уровень.
- [ ] Экспорт проекта = только видимые группы ВЕРХНЕГО уровня (вложенные —
      части экрана, не отдельные экраны).

## Группы v2, инкремент 2 (T0219) — z-order групп

- [ ] Группу можно перетащить в layers выше/ниже соседей (линия вставки);
      порядок отрисовки на канвасе меняется соответственно.
- [ ] Правый клик по группе → Order ▸ (Bring to front / forward / backward /
      to back) работает; Ctrl+[ / Ctrl+] на выбранной группе тоже.
- [ ] Один Ctrl+Z откатывает перестановку целиком.
- [ ] Экспорт/Render group уважает новый порядок слоёв.

## Группы v2, инкремент 1 (T0219) — фон групп

- [ ] У группы в инспекторе секция Background: Solid + цвет → заливка ВИДНА
      на канвасе под содержимым (рамка и подпись остались); None убирает.
      Ctrl+Z откатывает цвет.
- [ ] Render group / экспорт проекта: фон запечён в PNG тем же цветом.
- [ ] Правый клик по группе → Background ▸ — быстрая смена цвета.
- [ ] Порядок отрисовки не поменялся на старых проектах (единственное
      ожидаемое отличие: члены группы теперь рисуются одним слоем-блоком).

## Живые фиксы 2026-07-03 (по твоим репортам — перезагрузи страницу)

- [ ] Delete-клавиша удаляет выделенную ГРУППУ (с содержимым, один Ctrl+Z
      вернёт). Мульти-группы/смешанное выделение — придёт с T0227.
- [ ] Артефакт-полоса внизу канваса после утаскивания группы за край —
      исправлен (очистка в девайс-пикселях; виноват дробный масштаб Windows).
- [ ] Копирование/вставка (T0227, ПРИЗЕМЛИЛОСЬ, сервер перезапущен):
      Ctrl+C на любом выделении (элементы/группы/смешанное) → Ctrl+V —
      вставка со смещением +16, повторный Ctrl+V смещает дальше; один
      Ctrl+Z убирает вставку целиком; работает даже после удаления
      оригинала. Ctrl+D — дубликат на месте. Вставка картинки из буфера
      ОС как раньше (она в приоритете). Delete теперь работает на ЛЮБОМ
      выделении, включая смешанное (одна запись, один Ctrl+Z).
- [ ] Таскание двух групп разом (за рамку/подпись любой из них) — выделение
      не сбрасывается, едут обе (фикс 41b8c641).

## Полировка (T0224, приземлилась — сервер перезапущен)

- [ ] Выдели 2+ группы: инспектор показывает «N groups» + общие галки
      Visible/Clip (галка «неопределённая», если группы расходятся; клик
      выставляет всем разом, один Ctrl+Z).
- [ ] Группа С ФОНОМ выбирается кликом по пустому телу рамки (как fill во
      Figma); группа без фона — как раньше, клик сквозь в marquee.
- [ ] Суффикс в строке экспорта можно СТЕРЕТЬ на месте (пустое значение
      коммитится); имена элементов/групп пустыми стать не могут.
- [ ] Shift-клик диапазон теперь и в списке РЕГИОНОВ инспектора.
- [ ] Призрак обрезанного клипом куска СКРЫТ по умолчанию; зажми Alt при
      выделенном элементе — покажется.
- [ ] Перетащи 2+ файлов картинок разом: ОДНА запись в журнале, один Ctrl+Z.
- [ ] Layers при глубокой вложенности: группы свёрнуты по умолчанию, путь к
      выделению раскрывается сам; тонкие вертикальные направляющие уровней;
      ширина панели тянется за правый край (запоминается).
      НЮАНС: Ctrl+G создаёт группу СВЁРНУТОЙ (раскрой кареткой) — если
      хочешь как в Figma (сразу раскрытой), скажи.
- [ ] Кнопка Export project считает только ВИДИМЫЕ группы верхнего уровня.

## Панель истории (T0204, ночь 2026-07-03 — сервер перезапущен)

- [ ] Кнопка History в верхней панели (или клавиша ` — бэктик) открывает
      плавающую палитру как в Photoshop; по умолчанию скрыта, состояние
      запоминается.
- [ ] Список = человекочитаемые шаги («Add text», «Move 3 elements»…),
      текущая позиция подсвечена; шаги «в будущем» (после undo) — приглушены.
- [ ] Клик по ЛЮБОЙ строке прыгает в то состояние (хоть на 5 шагов назад
      одним кликом); прыжок обратим — Ctrl+Z/Ctrl+Y и повторный клик работают
      от новой позиции; Ctrl+Z-спам не дёргает панель.
- [ ] CLI-паритет: history-list / history-jump --seq N (для агента).

## Ночь 2026-07-03: воркер + роботы в истории (сервер перезапущен)

- [ ] ПЕРФ (T0202): Detect regions / Slice / Render group — ПЕРВЫЙ вызов как
      раньше (~0.5с), каждый следующий почти мгновенный (python больше не
      стартует заново: рендер 99→7.5мс, детект 429→19мс). Погоняй подряд.
- [ ] 🤖 (T0228): попроси меня подвинуть что-нибудь через CLI (или сам:
      node ai_studio/assets/canvas/cli.mjs move <id> ...) → в панели истории
      этот шаг помечен 🤖; твои шаги со страницы — без иконки; старые записи
      без иконки.

## Целостность операций (T0223, приземлился следом — сервер перезапущен ещё раз)

- [ ] Выдели рамкой ГРУППУ + пару свободных элементов → подвинь → ОДИН
      Ctrl+Z возвращает всё (раньше мешанина давала N записей). Если группа
      выделена вместе со своим же элементом — элемент двигается один раз.
- [ ] Выдели 2+ элемента → Ctrl+] / Ctrl+[ или правый клик → Order:
      блок двигается целиком, взаимный порядок внутри блока сохраняется
      (раньше на мультивыделении не работало вовсе).
- [ ] Ungroup: дети встают ровно в тот z-слот, где стояла группа (не
      выпрыгивают наверх); один Ctrl+Z собирает группу обратно точь-в-точь.
- [ ] ИЗВЕСТНЫЙ остаток: перетащить НЕСКОЛЬКО файлов картинок разом = пока
      N записей в журнале (по Ctrl+Z на файл) — batched addImages в T0224.

## ТЕКСТ на канвасе (T0222, приземлился только что — сервер уже перезапущен)

- [ ] Инструмент «T» в тулбаре: клик по канвасу ставит текст и сразу
      открывает редактирование. Esc отменяет, Ctrl+Enter/клик мимо — коммит.
- [ ] Дабл-клик по готовому тексту = правка на месте (textarea поверх);
      один Ctrl+Z откатывает правку целиком.
- [ ] Инспектор, секция Text: шрифт (Inter / Rubik / Bitter / JetBrains
      Mono — все с кириллицей; Rubik вместо Fredoka: у той нет кириллицы),
      вес, размер, межстрочный, выравнивание, цвет, ОБВОДКА, ЖЁСТКАЯ ТЕНЬ.
- [ ] Кириллица: набери «Играть» жирным с обводкой — рендер и вид на
      канвасе совпадают (Render group → сравни PNG с канвасом; лёгкий
      сдвиг глифов на 1–2px допустим, переносы строк обязаны совпадать).
- [ ] Текст ведёт себя как обычный элемент: тащится, входит в группу
      (Ctrl+G), z-order, глаз, marquee; в layers у него иконка «T».
- [ ] Ширина авто: перенос строки только явным Enter, авто-wrap НЕТ
      (v1 так задуман). Экспорт экрана ЗАПЕКАЕТ текст в PNG; отдельная
      строка экспорта на самом тексте пока не работает (v1.1) — ругается
      понятной ошибкой, не молчит.
- [ ] Драг элемента за границу группы теперь НЕ меняет членство
      (твой закон: вынос/внос только явно — layers/Ctrl+G/Ungroup).
      «Припаркованный» за рамкой элемент выбирается кликом и рамкой.

## Если что-то не так

Кидай пункт + что увидел (можно Copy ID объекта) — чиню. Файл можно удалить
после проверки.
