# Список проверок для лида — 2026-07-03 (вечер/ночь)

Всё уже закоммичено и живое на :8780. Нужен F5 перед проверкой.
Файл пополняется по мере приземления новых задач — свежее сверху.

---

## 00. ДНЕВНЫЕ ПОСАДКИ 2026-07-05 (после твоих решений)

**CorridorKey вшит в Alpha** (коммит a229f769, :8780 перезапущен —
нужен F5): выдели картинку на ЗЕЛЁНОМ со свечением → Alpha → метод
**CorridorKey (green glow)** → Apply (~15с честного ожидания, GPU).
- [ ] Попробуй на светящемся арте — сравни с key_matte.
- Не-зелёный ключ → громкий отказ с подсказкой; авто-роутер его
  никогда не выбирает; undo попиксельный как у других методов.
- Фоллоу-ап УЖЕ В РАБОТЕ: маджента через hue180-шим (найден
  экспериментом: поворот цвета на 180° обманывает green-модель,
  строго лучше «синей на мадженте») + поддержка регионов.
- Маджента-исследование: tmp/research_corridorkey_magenta_2026-07-05.md
  (в комьюнити пусто — цвет запечён в весах; наш шим = обход).

**Облачная видео-генерация** (твой вопрос про Gemini) — отчёт
tmp/research_video_api_2026-07-05.md:
- Veo (это и есть видео у Gemini) — технически умеет всё, но НЕ брать:
  фотореал-байас ломает плоский 2D и зелёный фон, минимум 4с =
  $0.60-1.60/клип, подписки с дневными лимитами.
- ПОБЕДИТЕЛЬ — гибрид: черновики локально ($0, 35с) + финалы на
  hosted WAN (fal.ai, ТА ЖЕ модель — ноль дельты стиля): 720p ~$0.16
  за 2с-клип → **~$0.50 за персонажа** (3 финала). Всё-облако на
  hosted WAN = ~$2.64/персонаж (вместо $30 у «дорогих» сервисов).
- [ ] Решение: пилот fal.ai на <$5 (10-20 финалов 720-1080p) — да/нет?

## 0. УТРЕННЯЯ СВОДКА 2026-07-05 — «анимация из арта и описания» ГОТОВА

**Главное (твой утренний тест):** F5 → выбери картинку → секция
**Animation** → в поле сверху напиши движение СЛОВАМИ (можно
по-русски: «медленно покачивается и чуть вращается») → **[Animate]**
(codex думает ~10-30с, он ВИДИТ саму картинку) → превью само начинает
играть. Потом напиши «сделай вдвое медленнее» → правится минимальным
патчем (проверено вживую: периоды ровно x2, амплитуды не тронуты).
Каждый Animate = одна запись в undo. То же из CLI: `animate <proj>
--element <el> --text "..."`.

- [ ] Прогони свой сценарий: арт → текст → превью → правка текстом.

**Вырезание из видео (R2) — вердикты готовы** (сайд-бай-сайды:
C:\projects\video_gen_experiment\phase3\r2\compare\):
- **CorridorKey — PASS**: единственный, кто сохраняет мягкое золотое
  свечение И убирает зелень с краёв. Твоя находка победила.
- MatAnyone — PARTIAL: самая стабильная альфа по кадрам, но зелёная
  кайма (не делает unmixing) — резерв для жёстких краёв.
- Наш key_matte — FAIL на свечении (как ты и жаловался), остаётся
  для непрозрачных спрайтов.
- [ ] Глянь wingtip_zoom_frame*.png — «money shot» сравнения.
- ВАЖНО про лицензии: CorridorKey = CC-BY-NC-SA (README разрешает
  обработку ассетов, нельзя перепродавать как сервис); MatAnyone =
  строго некоммерческая. Реши, ок ли это для пайплайна.

**Ускорение генерации — СДЕЛАНО, замерено честно:**
- Правда о бейслайне: «103с» были полу-тёплым запуском; в реальной
  рабочей сессии чистовик 480/33 = **~54с**.
- Черновой профиль 384px/25кадров = **~35.5с** за попытку движения;
  качество черновика — ACCEPT (взмах читается, стиль держится).
- Лесенка на одном сиде работает: черновик→чистовик совпадают по
  движению (codex-vision судья: STRONG). Итого: перебрать ~4 сида и
  отрендерить чистовой — **~3 минуты**.
- Файлы: draft/final_workflow_api.json + раздел в README эксперимента.
- SageAttention (ещё -15-30%) отложен — требует установки на копию;
  скажи, если 35с мало.

**T0263 — ПЕРВАЯ ВЕРСИЯ КОНВЕЙЕРА ГОТОВА** (коммит 170569af):
`ai_studio/assets/tools/video/` — стадии generate→frames→matte→sheet
отдельными папками, каждая перезапускаема, канвас не тронут (шов —
следующий инкремент, по твоему слову). Живой golden-прогон на крыльях
(черновик, сид 70263): генерация 79с (холодная) → 25 кадров → CorridorKey
42.7с (свечение сохранено) → спрайтшит 1920×1920, 5×5, 25 кадров.

- [ ] Открой и глянь спрайтшит:
      C:\projects\video_gen_experiment\video_runs\
      wings_golden_draft_seed70263_20260703T195721Z\sheet\*.png
      (рядом _sheet.json с мета: frame size / count / fps / провенанс).
- [ ] Одной командой весь конвейер:
      node ai_studio/assets/tools/video/run.mjs --image <png на зелёном>
      --text "the wings flap slowly" --profile draft
      (ComfyUI надо поднять заранее — команда в README эксперимента;
      конвейер громко подскажет, если сервер не поднят).
- Известное: у CorridorKey покадровый прогон → лёгкое мерцание альфы
  (caveat из R2); зелёные блёстки в паре кадров — это WAN нагенерил
  частицы, не баг вырезания. Ручки есть, если мешают.
- [ ] Решение после проверки: строить шов в канвас (кнопка на элементе
      → конвейер → спрайтшит обратно на канвас карточкой)?

---

## 1. ЧАТ-ПАНЕЛЬ (T0242) — главное новое

Кнопка **Chat** в верхней панели канваса → колонка справа (уживается с History и инспектором).

- [ ] **Первое сообщение**: выдели элемент, напиши «переименуй в Test» → Enter.
      Твой пузырь с чипом выделения, спиннер «Working…», ответ + чип `ops N–M`,
      канвас сам обновился, History показывает запись. Первый ответ думает
      заметно (холодный старт кодекса) — это норма.
- [ ] **Непрерывность**: второе сообщение «что ты только что переименовал?» —
      агент помнит без переобъяснений (живая codex-сессия).
- [ ] **Реальная операция**: выдели 2+ элемента → «выровняй выделенные по
      центру» → позиции реально меняются, чип `ops N–M`.
- [ ] **Cancel** во время спиннера → приглушённая заметка «Cancelled — the codex
      session survives…» (НЕ красная ошибка); следующее сообщение продолжает
      тот же диалог.
- [ ] **Clear** → инлайн-подтверждение → панель пустеет; следующее сообщение —
      новая сессия (прошлое не помнит). Старый транскрипт архивирован на диске.
- [ ] **Запрещённое**: «удали этот проект» → отказ; если в ответе мелькнёт
      delete-глагол — красный флаг под пузырём. Проект цел.
- [ ] **F5**: история чата подгружается из файла (чипы выделения не
      переживают перезагрузку — так задумано).
- [ ] Ограничение v1: одно сообщение за раз на проект (композер блокируется).

## 2. FIT-TO-CONTENT у промоушенов (уже проверял частично)

- [ ] Extract на большой картинке → «→ Recipe card» / «→ Style card»:
      рамка карточки обтягивает копию арта + 16px отступ (не 360×280).
      Пустые карточки из контекстного меню — по-прежнему дефолтного размера.

## 3. T0234 — guard от гонки history-jump (агентский, глазами не видно)

Реализован и покрыт тестами: undo/redo/history-jump из CLI агента теперь
ТРЕБУЮТ --expect-head N (свежая голова журнала); расхождение = громкий отказ
без записи. Страница не тронута. Это ответ на инцидент с осиротевшими
операциями. Проверка = доверие тестам; хочешь живую демонстрацию — скажи,
покажу отказ на живом проекте.

---

## 4. T0207 — чистка арта, БЭКЕНД готов (UI строится прямо сейчас)

Quantize (палитра до N цветов, опц. дизеринг) + Denoise (сила 1-3), альфа
байт-в-байт нетронута в обоих. Превью ничего не пишет; Apply = новый файл +
одна журнальная замена, Ctrl+Z возвращает оригинал попиксельно. Отчёт
до/после. Сьют 470. Через CLI уже можно попробовать:

    node ai_studio/assets/canvas/cli.mjs quantize <proj> --element <el> --colors 32 --preview tmp/q.png
    node ai_studio/assets/canvas/cli.mjs denoise <proj> --element <el> --strength 2 --preview tmp/d.png

UI ПЕРЕДЕЛАН по твоему живому фидбеку 2026-07-04 («кажется что это про
одно», «шум сбрасывает квантование», «почему не модальным окном») —
теперь ФОТОШОПНЫЙ паттерн: плавающие окна инструментов (коммит
46df7c4e, F5). Исследование: tmp/research_cleanup_ux_competitors/
redesign_2026-07-04.md.

- [ ] Выбери картинку → секция **Cleanup** → кнопки **[Quantize…]** и
      **[Denoise…]** открывают плавающее окно у левого края сцены;
      канвас виден, превью живое ПРЯМО НА АРТЕ (+ янтарный бейдж
      «preview — Apply to keep» над элементом).
- [ ] Quantize: строка «Current palette: N colors», слайдер стартует
      от текущего N; отчёт «palette N -> M, X% changed»; при 0%
      отчёт объясняет почему и что крутить.
- [ ] Denoise: 0|1|2|3 (0 = выкл, дефолт), подсказка «0 off · 1 light
      · 3 strong».
- [ ] **Hold to see original** зажал — оригинал, отпустил — превью.
- [ ] **Apply** применяет (один шаг undo, Ctrl+Z попиксельно назад;
      после Apply в секции строка «Applied: … — Ctrl+Z reverts»);
      **Cancel / × / Esc** — ноль следов.
- [ ] Пока окно открыто — вторая кнопка задизейблена (только одно
      «незакоммиченное» превью за раз, как в Фотошопе). Оба
      инструмента = последовательно: Apply одного → окно второго.
      Рекомендуемый порядок: denoise → quantize.
- [ ] Смена выделения / удаление элемента закрывает окно и превью без
      следов; на повёрнутом/флипнутом кнопки задизейблены (R7).

## 5. T0233 slice-9 — ДИЗАЙН ГОТОВ, нужно твоё решение (2 вопроса)

Док: tmp/design_T0233_slice9_2026-07-03.md. Кратко: старый viewer-модуль —
фантом (в живом дереве его нет), семантика берётся из движка (nt_atlas
slice9_lrtb) без порта кода; модель = аддитивный element.slice9
{left,top,right,bottom} в пикселях источника; slice-9 подменяет шаг
«растянуть в бокс» в обоих рендерах, поэтому поворот/флип компонуются
бесплатно; общая rect-математика JS + питон-близнец, паритет запинен
пиксельным тестом на асимметричной 9-цветной фикстуре. 2 инкремента.

- [ ] **Вопрос 1**: углы фиксированного размера в пикселях источника
      (стандарт, как в движке) — подтверждаешь? (альтернатива «углы
      скейлятся с боксом» не рекомендуется)
- [ ] **Вопрос 2**: v1 UI = числовые поля L/T/R/B в инспекторе (перетаскиваемые
      направляющие на канвасе — v2). Хватит, чтобы «проверить работает ли»?

ОБНОВЛЕНИЕ (ты сказал «работай», не дожидаясь): имплементацию ЗАПУСТИЛ с
рекомендованными дефолтами — углы фикс в пикселях источника (вопрос 1 = да),
инкремент 1 (математика+рендеры+CLI/API) от вопроса 2 не зависит. Если
хочешь иначе — вето дёшево, схема аддитивная, скажи и переделаю.

ОБНОВЛЕНИЕ 2 (твоё «важно чтобы я мог скейлить края»): множитель размера
углов/краёв (аналог движкового slice9_scale) включён СРАЗУ в первый
инкремент, не в v2: element.slice9.scale (по умолчанию 1) — scale 2 = углы
вдвое жирнее, 0.5 = тоньше; исходные пиксели не трогаются, только размер
отрисовки. В CLI это --scale, в инспекторе (инкремент 2) будет поле.

ИНКРЕМЕНТ 1 ГОТОВ (закоммичен, :8780 перезапущен, сьют 470 -> 488).
Паритет страница/PIL-рендер запинен пиксельным тестом на асимметричной
9-цветной фикстуре (scale 1 и 2) + тест композиции с поворотом и флипом.
Проверка через CLI (страница покажет то же самое):

    node ai_studio/assets/canvas/cli.mjs slice9-set <proj> --element <el> --left 24 --top 24 --right 24 --bottom 24
    # растяни элемент на странице гизмо: углы 24px чёткие, края тянутся по
    # одной оси, центр — по обеим
    # ... --scale 2   -> углы/края вдвое жирнее (пиксели источника не трогаются)
    # ... --clear     -> старое поведение (растяжение размазывает углы)
    # --left 60 --right 60 на источнике 100px -> громкий отказ (инвариант движка)

ИНКРЕМЕНТ 2 ГОТОВ (закоммичен, сьют 489; site JS — нужен только F5):

- [ ] Выбери картинку-панель → секция **Slice-9** в инспекторе (после
      Cleanup): поля L/T/R/B уже с разумными значениями → **Enable**
      (одна запись в undo).
- [ ] Растяни элемент гизмо → углы фиксированы, края тянутся по одной
      оси, центр — по обеим.
- [ ] **Scale** = 2 → углы/края вдвое толще (твоя ручка «больше/меньше»);
      0.5 → тоньше. Пиксели источника не меняются.
- [ ] **Clear slice-9** → старое поведение (растяжение размазывает углы).
- [ ] Невалидные инсеты (L+R >= ширины источника) → громкая ошибка,
      ничего не записано.
- [ ] Экспорт/рендер группы с такой панелью выглядит ровно как на
      странице (паритет запинен тестами, но глазами тоже глянь).

---

## 6. БОЛЬШОЕ РЕВЬЮ (T0253/T0254) — ГОТОВО, читай синтез

Полный мастер-док: **tmp/review_2026-07-04_SYNTHESIS.md** (24 пункта в 3
тира, на русском). Шесть разборов-первоисточников: tmp/review_T0253_*.md
и tmp/review_T0254_*.md.

Вердикт одной строкой: инструмент крепкий, кости дисциплинированные
(паритет, журнал, громкие ошибки — не трогать), но он перерос два своих
допущения: «один писатель» (чат+CLI сделали стор многописательным без
лока) и «превью на полных пикселях».

Tier 1 (корректность + дешёвое) запущен В РАБОТУ без тебя — там нет
вкусовых решений: write lock, коды ошибок 404/409, wheel-zoom через rAF,
list --summary, строгий bool-парсер CLI, element-set --w/--h.

- [x] **Вопросы РЕШЕНЫ тобой 2026-07-04** («+card не нужно, всё остальное ок»):
  1. key_matte dead-pass removal — golden-тест ЗАБЛОКИРОВАЛ удаление, см.
     новый вопрос 6-bis ниже. Chebyshev-часть сделана.
  2. Chebyshev-унификация + рекалибровка route — СДЕЛАНО (T0258, коммит
     41594a0e): общий lib/color.py, route 80→74 (вердикты не изменились),
     border-key = мода везде. Питон-тесты 53/53.
  3. Snapshots/journal с YandexDisk в локальный кэш — СДЕЛАНО (T0259,
     коммиты 6986a581+f3723576): журнал/снапшоты/.lock живут в
     C:/Users/ROG/AppData/Local/ai_studio/canvas_cache (НЕ в repo tmp —
     tmp подметается), project.json и files/ остаются в синке. Миграция
     сама, при первом обращении; проверено вживую на Demo (213 снапшотов
     переехали, undo работает). На другой машине: правки продолжаются от
     синкнутой головы, а undo в несинкнутую историю громко отказывает —
     принятый тобой трейдофф.
  4. Стиль-карточка → фиолетовый #9d7fd8 — СДЕЛАНО (закоммичено, F5).
  5. Кнопка «+ Card» — ОТКЛОНЕНО тобой (дубль правого клика). Снято.

- [ ] **6-bis. НОВЫЙ вопрос по key_matte (находка golden-теста)**: проходы
      bleed+repair НЕ мёртвые на фигурах с дыркой (кольцо): сегодня они
      принудительно стирают едва видимые пиксели с альфой 1–12 (24/4096 на
      кольце 64px; макс |dA|=12). На простом мягком крае — 0 отличий.
      Удаление даёт 2.9x (6.3с → 2.2с на 1254px). Варианты:
      (a) оставить как есть (сделано сейчас — безопасно, но медленно);
      (b) spill-gate: гонять хвост только когда есть спилл (доработка);
      (c) принять слайвер-разницу и удалить (2.9x, кромка чуть шумнее).
      Рекомендация воркера и моя: (b) или (c) — слайверы с альфой ≤12
      это остатки ключа, их стирание скорее полезно, НО решение за тобой.

## 7. АНИМАЦИИ (T0256) — синтез готов, план двухтрековый

Мастер-док: **tmp/research_T0256_SYNTHESIS.md** (RU). Кратко:
- **Track A (процедурка)** — готова к стройке НА ЭТОЙ НЕДЕЛЕ на нулевой
  новой инфре: спека v1 (осцилляторы/кейфреймы над offset/rot/scale/
  opacity), превью на канвасе, codex правит спеку минимальным патчем
  («крылья медленнее» = одно число), запекание кадров через существующий
  PIL-рендер → твой спрайтшит. Твоя нарезка даёт мульти-парт бесплатно.
- **Track B (генератив, WAN I2V + CorridorKey)** — сначала спайк-гейт:
  установка (уже идёт, T0257) + 3 эксперимента на крыльях; стройка
  только при pass, в одной удаляемой папке ai_studio/assets/tools/video/
  со стадиями generate/matte/frames/sheet, шов в канвас — последним.
- Оба трека сходятся в один артефакт: RGBA-кадры → спрайтшит → флипбук
  (у движка аним-рантайма нет by design — проверено).
- Твои решения, уже учтённые: локальный маршрут (не облако за $), твой
  CorridorKey = основной экстрактор, изоляция+декомпозиция.

- [x] **Вопрос A1**: РЕШЕНО тобой 2026-07-04 — да, процедурка первой.
- [x] **Вопрос A2**: РЕШЕНО тобой 2026-07-04 — да, развести:
      процедурка = element.animation-блоб, генеративка = карточка.

ОБНОВЛЕНИЕ: Track A ИНКРЕМЕНТЫ 1+2 ГОТОВЫ (829faef1 + 59720ac6, сьют
540, :8780 перезапущен; инкремент 2 = site JS, нужен только F5).
Бэкенд: спека v1 (осцилляторы + кейфреймы над off_x/off_y/rot/scale/
opacity), чистый сэмплер, op + CLI + API. Плюс статическая
ПРОЗРАЧНОСТЬ элемента (element.opacity, паритет страница/PIL запинен).

- [ ] Выбери картинку → секция **Animation** в инспекторе (после
      Slice-9) → **Add sample animation** → элемент мягко покачивается
      (спрайт анимируется, а рамка выделения стоит на месте — так
      задумано: геометрия не мутирует, экспорт/хиты по статик-боксу).
- [ ] **Play/Stop** — чистое view-state, ноль записей в историю.
- [ ] **Edit JSON** → поменяй amplitude/period_ms → Save → превью
      меняется. Кривой JSON → громкая ошибка, текст не теряется.
- [ ] Два элемента с одинаковой спекой играют В ФАЗЕ (общие часы).
- [ ] Удали/Ctrl+Z играющий элемент → превью само гаснет, ничего не
      ломается.
- [ ] **Clear animation** (двухшаговое подтверждение) → одна запись в
      undo.
- [ ] Прозрачность: element-set --opacity 0.5 через CLI (или чат) →
      F5 → полупрозрачный и на странице, и в экспорте группы.
- [ ] Чат-агент может: «добавь этой картинке покачивание» / «сделай
      крылья медленнее» (он правит JSON-спеку минимальным патчем).

Инкремент 3 (запекание кадров + спрайтшит + python-близнец сэмплера) —
после твоего вердикта по превью.

# АНИМАЦИИ: СИНТЕЗ ИССЛЕДОВАНИЙ + ПЛАН (2026-07-05, после твоего пивота на видео)

Два дока: tmp/research_video_anim_practice_2026-07-05.md (как делают,
что реально на 12GB) + tmp/design_video_anim_canvas_2026-07-05.md
(как это живёт на канвасе). Сходятся в один план:

- **Карточка анимации** (как recipe): исходный арт + текст движения +
  полоска кейфреймов (позы рисуешь/генеришь на канвасе как обычные
  картинки-члены карточки). Одна карточка = одно состояние (idle/
  walk/attack); идентичность персонажа между карточками — общий
  исходник + style_ref, дальше LoRA.
- **Результат = редактируемая ПОСЛЕДОВАТЕЛЬНОСТЬ КАДРОВ** (не
  запечённый лист): element.flipbook {кадры с флагом kept, fps,
  режим once/loop/pingpong}. Обрезка/удаление кадров = обычные
  журналируемые операции с undo. Спрайтшит = производный ЭКСПОРТ из
  оставленных кадров.
- **Твоя «сцена» подтверждена дизайном**: отдельный РЕЖИМ анимации
  (как изоляция region-edit): большой флипбук + таймлайн миниатюр
  внизу (обрезка, удаление, плейхед) + транспорт (▶ once/loop/
  pingpong, fps) + полоска кейфреймов + Generate. Esc — назад.
- **Модальности управления** (из практики): текст-I2V — только для
  эмбиента (покачивание/FX); рабочая лошадка — **FLF** (первый=
  последний кадр → бесшовный луп; поза A→поза B → инбитвининг твоей
  раскадровки); идентичность — **LoRA на персонажа** (12GB тянет,
  ~25-30 картинок, 2.5-4ч); ToonCrafter — пилот. ToonComposer/VACE-
  перенос — НЕ планировать (57GB / разваливается).

ИНКРЕМЕНТЫ (T0265 активен, стартует как освободятся файлы канваса):
1. Ядро: арт → карточка → Generate (черновик 35с) → RGBA-флипбук
   играет на канвасе. (На текущем I2V.)
2. РЕЖИМ анимации: таймлайн + обрезать/удалить кадры + fps +
   once/loop/pingpong (твои требования, все журналируемые).
3. FLF: лупы из одного кадра + раскадровка по 2+ кейфреймам (нужен
   FLF-workflow в эксперименте — T0266).
4. LoRA-эксперимент + ToonCrafter-пилот (T0266, backlog).

ТРИ ВКУСОВЫХ ВОПРОСА — РЕШЕНЫ ТОБОЙ 2026-07-05:
- [x] Дефолтный matte = **corridorkey** («corridor»).
- [x] Экспорт = **НАБОР КАРТИНОК** (кадры одного размера, выровнены
      самим видео-холстом — смещения/пивоты НЕ хранятся; спрайтшит-
      упаковка остаётся опцией). Пер-кадровый трим прозрачных полей
      с оффсетами — только если когда-нибудь попросишь отдельно.
- [x] Ping-pong = **режим проигрывания** (play_mode в мета/превью,
      кадры не дублируются).

# ПЛАН ПО ВИДЕО/АНИМАЦИИ (лид одобрил 2026-07-05: «нужны задачи и план»)

1. T0261 (В РАБОТЕ): сборка CorridorKey + MatAnyone (разрешение дано)
   → сравнение на кадрах со свечением vs key_matte (края / свечение /
   грязный зелёный / мерцание альфы между кадрами / скорость) →
   вердикты; если оба не годятся — шортлист альтернатив (RVM,
   BiRefNet, SAM2-комбо, ComfyUI-кейеры).
2. T0262 ИССЛЕДОВАНИЕ ГОТОВО (tmp/research_T0262_speedup_2026-07-05.md):
   - Неожиданное: TeaCache/MagCache НЕ наш инструмент — они дают 2x на
     40-шаговых прогонах, а у нас уже дистиллят 4-8 шагов (кэшировать
     нечего, ломается плавность движения). Единственный стоящий
     ускоритель — SageAttention (1.15-1.4x).
   - Пакет №1 (БЕЗ установок, только настройки): черновик 384px +
     25 кадров + 4 шага + пачка сидов → прогноз ~35-57с вместо 103с.
   - Пакет №2 (+ SageAttention на КОПИИ portable): ~30-50с.
   - Лесенка черновик→чистовик: ТОТ ЖЕ движок WAN с зафиксированным
     сидом (черновик 384 → принятый сид перегнать в 480/768). Чужой
     быстрый движок (LTX/5B) черновиком НЕ брать — не предсказывает
     финал.
   - Применение пакета №1 — сразу после того, как T0261 освободит
     GPU/папку эксперимента.
3. T0263 (BACKLOG, гейт = вердикты 1+2): ПЕРВАЯ ВЕРСИЯ конвейера —
   стадии generate→matte→frames→sheet отдельными папками (изоляция и
   декомпозиция — твои требования), шов в канвас ПОСЛЕДНИМ.
4. Track A инкремент 3 (запекание процедурки в кадры→спрайтшит) —
   после твоего вердикта по превью (пункт 7 выше). Оба трека сходятся
   в один артефакт: RGBA-кадры → спрайтшит → флипбук.

# В РАБОТЕ СЕЙЧАС (2026-07-04)

- ПРИЗЕМЛИЛИСЬ и закоммичены: Tier1-бэкенд (write lock + 404/409 +
  list --summary + строгий bool + element-set --w/--h/--x/--y; сьют
  506; :8780 перезапущен, smoke ок) и T0258 (см. выше).
- T0257: ВСЕ 3 ФАЗЫ ГОТОВЫ, вердикт УСЛОВНЫЙ GO — читай
  tmp/t0257_phase3_report.md. Кратко:
  - Стек: ComfyUI + WAN 2.2 I2V Q4 + Lightning (23.9GB, Apache-2.0) в
    C:\projects\video_gen_experiment; smoke 33 кадра 480x480 = 218с
    холодная / 103с тёплая, VRAM с запасом.
  - R1 дрейф стиля: PASS на реальных крыльях. Ключ: жёсткий префикс
    «2d game art, flat colors, no photorealism» даёт +0.7-0.8 балла
    (4.17/5 у codex-судьи). Плоская тестовая фигура ДО крыльев
    превращалась в фотореал-существо — префикс обязателен.
  - R3 цена правки: PASS — 3 правки («медленнее», «сильнее взмах»,
    «ровнее») все с первой попытки, ~2 мин каждая.
  - R2 вырезание+свечение: ЗАБЛОКИРОВАНО разрешениями — воркеру
    нельзя собирать свежесклонированные внешние репо. CorridorKey уже
    склонирован и ждёт: запусти сам
    C:\projects\video_gen_experiment\tools\CorridorKey\Install_CorridorKey_Windows.bat
    (или скажи мне «разрешаю установку» — доделаю). Наш key_matte на
    сгенерённом кадре честно показал свою задокументированную слабость:
    тёмная кайма на краю свечения. Зелёный у WAN остаётся чистым
    (дрейф 11/441) — вырезание реалистично.
  - Рекомендация: строить generate→frames→sheet сейчас (для
    непрозрачных спрайтов хватает key_matte), matte-стадию со
    свечением — после R2.
- T0259 (Opus): перенос journal/snapshots/.lock в локальный кэш
  (store.mjs + studio_config; project.json и files/ остаются в синке).
- T0260 (Opus): анимация Track A инкремент 1 — спека v1 + чистый
  сэмплер + element.opacity в обоих рендерах + ops/CLI/API (превью на
  канвасе и инспектор = инкремент 2).

## 8. T0255 «тысячи объектов» — мемоизация + каллинг ГОТОВЫ

Твой таргет «может быть тысячи объектов»: дерево мемоизировано (рендер
1000 эл. 12.6→0.15мс) и теперь рендер СКИПАЕТ всё вне экрана (коммит
4fa7928b): на сетке 1024 элементов при зуме — 2-4x быстрее, без зума —
без регрессии. Пиксельный паритет доказан байт-в-байт скриншотами.
Глазами: просто работай как обычно — ничего не должно мигать/пропадать
на краях экрана при пане/зуме (запас 64px). Если увидишь пропажу у
края — это баг каллинга, зови. Остаток задачи (диета payload'ов
мутаций) требует обсуждения контракта API с тобой — не строю без тебя.

# Мелкие чистяки, сделаны попутно (глазами проверять нечего)

- Удалена мёртвая ветка attempt.skip в generateFromRecipe (осталась от
  снятого отказа gemini+refs).
- NUL-байт в сентинеле tree.mjs заменён на  -escape — grep/sed
  перестали считать файл бинарным.
