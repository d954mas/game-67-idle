# Extensions

Extensions are optional code outside the default template and reusable feature
catalog. The repository currently has no active extensions. A future extension
must declare its owner, build opt-in, validation route, and support status.
