# Extensions

Extensions are optional code outside the default template and reusable feature
catalog. Nothing below `extensions/experimental/` is discovered, packaged,
built, tested, or supported by default.

Experimental extensions require an explicit module-specific build opt-in. Their
own README is the authority for current capability and known defects. Moving an
extension out of `experimental/` requires a separate reviewed production task.
